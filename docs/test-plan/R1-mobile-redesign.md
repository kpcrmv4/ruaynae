# R1 · รีดีไซน์มือถือ (วันนี้ + เมนูตาม role + บันทึกต่อเนื่อง) — ตารางตรวจรับ

> ตาม `DESIGN.md §4.1` (คำสั่งเจ้าของ 31 ส.ค. 2569) · **ไม่มีการแก้ฐานข้อมูล/สิทธิ์/endpoint**
> `tsc` เขียวและ `next build` เขียว **ไม่นับ**เป็นหลักฐานของแถวใดในนี้

**สถานะ:** `☐` ยังไม่ตรวจ · `✅` ผ่านโดยมีคำสั่งรันได้รองรับ · `❌` ตก · `⚠️` blocked · `👤` ต้องคนตรวจ

> 👤 ของรอบนี้ = แถวที่ต้องเปิดเบราว์เซอร์กับฐานข้อมูลจริง — เครื่องที่ทำรีดีไซน์รอบนี้
> ไม่มี `.env.local` ของโปรเจ็คจริง จึงตรวจได้เฉพาะระดับโค้ด/บิลด์ · ไล่ปิดตอนรัน E2E
> บนเครื่องพัฒนา (ทั้งสองธีม · 390/768/1440 ตาม CLAUDE.md §16)

---

## 0 · สำรวจพื้นผิวที่แตะ

| นับอะไร | จากไหน | จำนวน |
|---|---|---|
| Routes ที่แก้ | `/` · `/entry` · `/attendance` · `/approvals` · `/ledger` | **5** |
| Shell ที่แก้ | `nav.ts` · `bottom-nav.tsx` · `sidebar.tsx` · `(app)/layout.tsx` · `metric.tsx` | **5** |
| API ใหม่ | — (ใช้ endpoint เดิมทั้งหมด) | **0** |
| Query ใหม่ (อ่านอย่างเดียว) | pending count (เมนู) · รายการวันนี้ +count · attendance วันนี้ · pending เก่าสุด · rejected ของฉัน · attendance เมื่อวาน · รายการที่ฉันคีย์วันนี้ | **7** |

## R1-NAV — เมนูและแผ่นบันทึกประจำวัน

| ID | Trigger | Role | สิ่งที่ต้องเห็น | สถานะ |
|----|---------|------|----------------|--------|
| R1-NAV-01 | เปิดแอปจอ 390 | supervisor | แถบล่าง: วันนี้ · คนเข้าไซต์ · [+] · รายการ · เพิ่มเติม | 👤 |
| R1-NAV-02 | เปิดแอปจอ 390 | owner | แถบล่าง: วันนี้ · รออนุมัติ(ตัวเลขแดง=จำนวน pending) · [+] · รายการ · เพิ่มเติม | 👤 |
| R1-NAV-03 | แตะ [+] | ทั้งสอง | แผ่น "บันทึกประจำวัน" — supervisor 3 แถว (ไม่มีรายรับ/เบิก... เบิกเป็นของเจ้าของ) · owner 4 แถว | 👤 |
| R1-NAV-04 | แตะ "บันทึกรายรับ" ในแผ่น | owner | `/entry?kind=income` เปิดฟอร์มฝั่งรายรับทันที ไม่ต้องกดสลับ | 👤 |
| R1-NAV-05 | แตะ เพิ่มเติม | ทั้งสอง | การ์ดชื่อผู้ใช้+role · เมนูที่เหลือครบ (เทียบ `navFor` ลบช่องในแถบ) · ปุ่มธีม · ออกจากระบบ | 👤 |
| R1-NAV-06 | ดู sidebar เดสก์ท็อป | owner | เมนู "รออนุมัติ" มีป้ายแดงจำนวนค้าง · จำนวนตรงกับหน้า `/approvals` | 👤 |
| R1-NAV-07 | supervisor เปิดหน้า | supervisor | query นับ pending **ไม่ถูกยิง** (โค้ด: สาขา `Promise.resolve`) — ยืนยันจากโค้ด `(app)/layout.tsx` | ✅ |

## R1-TODAY — หน้า "วันนี้"

| ID | Trigger | Role | สิ่งที่ต้องเห็น | สถานะ |
|----|---------|------|----------------|--------|
| R1-TODAY-01 | เปิด `/` | owner | การ์ดงานวันนี้ 3 แถว: รออนุมัติ (จำนวน+ยอด+เก่าสุดค้าง n วัน) · คนเข้าไซต์วันนี้ (x/y ไซต์ · รวมกี่คน) · รายการเงินวันนี้ (เข้า/ออก) | 👤 |
| R1-TODAY-02 | เปิด `/` | supervisor | 2–3 แถว: ลงชื่อ (ลงแล้วกี่คน) · บันทึกรายจ่าย (กี่รายการ·ยอด·รออนุมัติกี่) · ตีกลับ (เฉพาะเมื่อมี) | 👤 |
| R1-TODAY-03 | ทุกแถวการ์ด | ทั้งสอง | แตะแล้วไปหน้างานนั้น (`/approvals` `/attendance` `/entry` `/ledger?...`) — ไม่มีตัวเลขทางตัน | 👤 |
| R1-TODAY-04 | ยอด "รายการเงินวันนี้" | owner | เท่าผลบวกจริงของรายการวันนี้ที่ไม่ถูกตีกลับ (เทียบกับ `/ledger?from=วันนี้&to=วันนี้`) | 👤 |
| R1-TODAY-05 | วันมีรายการ > 200 | owner | ป้ายติด "(บางส่วน)" แทนยอดขาดเงียบ ๆ (โค้ด: `sumsPartial`) | ✅ |
| R1-TODAY-06 | แถบสถานะไซต์ | ทั้งสอง | เหลือ 3 ช่อง ไม่มีช่องรออนุมัติซ้ำกับการ์ดงานวันนี้ | 👤 |

## R1-FLOW — บันทึกต่อเนื่อง

| ID | Trigger | Role | สิ่งที่ต้องเห็น | สถานะ |
|----|---------|------|----------------|--------|
| R1-FLOW-01 | เปิด `/entry` จอ 390 | ทั้งสอง | หมวดเป็นชิป (สูง ≥44px) · จ่ายด้วย 2 ปุ่ม · ปุ่มบันทึกลอยเหนือแถบล่าง มียอดเงินบนปุ่ม | 👤 |
| R1-FLOW-02 | กดบันทึกสำเร็จ | ทั้งสอง | ช่องยอดเงินว่าง+ได้โฟกัส · ไซต์/หมวด/วันที่/วิธีจ่ายค้างเดิม · ลิสต์ "วันนี้คีย์แล้ว" ใต้ฟอร์มอัปเดต+สถานะถูก | 👤 |
| R1-FLOW-03 | `/attendance` มีคนลงเมื่อวาน | ทั้งสอง | ปุ่ม "เหมือนเมื่อวาน — ลงชื่อ n คนเดิม" (นับเฉพาะคนที่ยัง active และยังไม่ลงวันนี้) | 👤 |
| R1-FLOW-04 | กดเหมือนเมื่อวาน โดยมี 1 คนถูกลงไซต์อื่นแล้ว | ทั้งสอง | คนที่ลงได้ถูกลงครบ · toast บอก "ลงชื่อแล้ว n · ข้าม k" · ไม่มีแถวซ้ำ (unique ของ DB กัน) | 👤 |
| R1-FLOW-05 | เหมือนเมื่อวาน copy ครึ่งวัน | ทั้งสอง | คนที่เมื่อวานครึ่งวัน → วันนี้ครึ่งวัน · **OT ไม่ถูกคัดลอก** (โค้ด: `postSignIn(..., 0)`) | ✅ |
| R1-FLOW-06 | เลื่อนลิสต์คนยาว ๆ | ทั้งสอง | แถบสรุปลอยล่างเห็นตลอด: จำนวนคน (+ค่าแรงเฉพาะ owner) · supervisor ไม่เห็นเงิน | 👤 |
| R1-FLOW-07 | สคริปต์ตรวจเดิม | — | `verify-attendance.mjs` ยังหา `data-day-wage` เจอ (ย้ายไปอยู่แถบสรุป) — `grep -n "data-day-wage" src/app/\(app\)/attendance/attendance-client.tsx` | ✅ |
| R1-FLOW-08 | `/approvals` จอ 390 | owner | ปุ่มตีกลับ/อนุมัติเต็มแถว (อนุมัติใหญ่กว่า) · รูปสลิป 64px · รายการค้าง ≥2 วันขึ้น "ค้าง n วัน" สีแดง | 👤 |
| R1-FLOW-09 | `/ledger` | ทั้งสอง | กลุ่มตามวัน หัว "วันนี้/เมื่อวาน/วันที่" + ยอดสุทธิของวัน (ไม่รวมตีกลับ) | 👤 |
| R1-FLOW-10 | กด "โหลดเพิ่ม" จนวันถูกตัดกลาง | ทั้งสอง | กลุ่มแรกของหน้าถัดไปและกลุ่มท้ายที่ยังมีต่อ **ไม่โชว์ยอด** (โค้ด: `netTrustworthy`) | ✅ |

## R1-SITE — ปุ่มลัดบนหน้ารายละเอียดไซต์

| ID | Trigger | Role | สิ่งที่ต้องเห็น | สถานะ |
|----|---------|------|----------------|--------|
| R1-SITE-01 | เปิด `/sites/<id>` ของไซต์ `active` | owner | 4 ปุ่ม: บันทึกรายจ่าย · บันทึกรายรับ · ลงชื่อคนเข้าไซต์ · รายการของไซต์นี้ | 👤 |
| R1-SITE-02 | เปิด `/sites/<id>` ของไซต์ `active` | supervisor | 3 ปุ่ม (ไม่มีบันทึกรายรับ — หัวหน้าไซต์คีย์รายรับไม่ได้อยู่แล้ว `INCOME_FORBIDDEN`) | 👤 |
| R1-SITE-03 | ไซต์สถานะ `done`/`cancelled` | ทั้งสอง | เหลือปุ่มเดียว "รายการของไซต์นี้" — ปุ่มบันทึก/ลงชื่อหายไป (ปลายทางไม่มีไซต์นี้ในกล่องเลือก) | 👤 |
| R1-SITE-04 | ไซต์สถานะ `paused` | ทั้งสอง | มีปุ่มบันทึก แต่**ไม่มี**ลงชื่อคนเข้าไซต์ (`/attendance` รับแค่ active/planning) | 👤 |
| R1-SITE-05 | แตะ "บันทึกรายจ่าย" | ทั้งสอง | `/entry?site=<id>` เปิดมาโดยกล่องไซต์เลือกไซต์นั้นไว้แล้ว | 👤 |
| R1-SITE-06 | แตะ "บันทึกรายรับ" | owner | เปิดฝั่งรายรับ + ไซต์นั้นถูกเลือก · สลับกลับเป็นรายจ่ายแล้วไซต์**ยังเป็นไซต์เดิม** | 👤 |
| R1-SITE-07 | ยิง `/entry?site=<uuid ที่ไม่มีสิทธิ์/ปิดงานแล้ว>` ตรง ๆ | supervisor | ตกไปใช้ค่าเริ่มต้นเดิม ไม่ระเบิด · กล่องไซต์แสดงไซต์ที่จะบันทึกจริงเสมอ (โค้ด: ตรวจกับลิสต์ที่ RLS คืน) | ✅ |
| R1-SITE-08 | แตะ "รายการของไซต์นี้" | ทั้งสอง | `/ledger?site=<id>` + แถบ "เฉพาะไซต์ \<ชื่อ\>" + ปุ่ม "ดูทุกไซต์" | 👤 |
| R1-SITE-09 | อยู่ใน ledger ที่กรองไซต์ แล้วกดชิป "รออนุมัติ" | ทั้งสอง | ยังกรองไซต์เดิมอยู่ (แถบไม่หาย) — ตัวกรองไม่หลุดเงียบ ๆ | 👤 |
| R1-SITE-10 | อยู่ใน ledger ที่กรองไซต์ แล้วพิมพ์ค้นหา | ทั้งสอง | ผลลัพธ์ยังอยู่ในไซต์เดิม (hidden input ของ `extra`) | 👤 |
| R1-SITE-11 | กด "ดูทุกไซต์" | ทั้งสอง | ตัวกรองไซต์หายอย่างเดียว · สถานะ/คำค้น/ช่วงวันที่ที่ตั้งไว้ยังอยู่ | 👤 |

## R1-ATT — คนที่ลงชื่อไซต์อื่นไปแล้ววันนี้ (เพดาน 1 วัน/คน/วัน)

| ID | Trigger | Role | สิ่งที่ต้องเห็น | สถานะ |
|----|---------|------|----------------|--------|
| R1-ATT-01 | คน A ลงเต็มวันที่ไซต์ 1 แล้ว เปิดไซต์ 2 | owner | แถวคน A เป็นสีเทา · ใต้ชื่อขึ้น "วันนี้อยู่ ‹ไซต์ 1› (เต็มวัน)" สีแดง · ป้าย "ลงครบวันแล้ว" · **ไม่มีปุ่มเข้าไซต์** | 👤 |
| R1-ATT-02 | คน B ลงครึ่งวันที่ไซต์ 1 แล้ว เปิดไซต์ 2 | owner | ใต้ชื่อขึ้น "วันนี้อยู่ ‹ไซต์ 1› (ครึ่งวัน)" · ป้าย "ลงได้ครึ่งวัน" แทนช่องติ๊กครึ่งวัน · ปุ่มยังกดได้ | 👤 |
| R1-ATT-03 | กดเข้าไซต์ต่อจาก R1-ATT-02 | owner | บันทึกสำเร็จเป็น **ครึ่งวัน** (0.5+0.5 = 1 วันพอดี) ไม่ใช่ถูกปฏิเสธ | 👤 |
| R1-ATT-04 | หัวหน้าไซต์ที่ดูแล 2 ไซต์ · คนลงที่ไซต์แรกแล้ว | supervisor | เห็นป้ายเหมือนเจ้าของ (RLS ให้อ่าน attendance ของไซต์ที่ตัวเองดูแล) | 👤 |
| R1-ATT-05 | คนไปอยู่ไซต์ที่หัวหน้าไซต์คนนี้ **ไม่ได้ดูแล** | supervisor | ไม่มีป้าย (มองไม่เห็นตาม RLS) แต่กดแล้ว**ต้องถูกปฏิเสธ**ด้วยข้อความ "วันนี้คนนี้ถูกลงชื่อที่ไซต์อื่นไปแล้ว…" — guard ที่ฐานข้อมูลยังเป็นตัวตัดสิน | 👤 |
| R1-ATT-06 | ปุ่ม "เหมือนเมื่อวาน" ขณะมีคนเต็มวันที่อื่น | ทั้งสอง | ตัวเลขบนปุ่ม**ไม่นับ**คนนั้น · กดแล้วไม่มีการยิงที่รู้อยู่แล้วว่าจะล้ม | 👤 |
| R1-ATT-07 | เมื่อวานเต็มวัน แต่วันนี้ไปครึ่งวันที่อื่นแล้ว | ทั้งสอง | "เหมือนเมื่อวาน" ลงให้ **ครึ่งวัน** (ตัดยอดพอดีโควตา) ไม่ใช่ยิงเต็มวันแล้วนับเป็นข้าม (โค้ด: `Math.min(..., capacityOf())`) | ✅ |
| R1-ATT-08 | ป้ายกับ guard ขัดกัน | — | ป้ายเป็นคำแนะนำเท่านั้น · ห้ามลบ/อ่อนข้อ guard `WORK_UNITS_EXCEEDED` ที่ฐานข้อมูล — `grep -n "WORK_UNITS_EXCEEDED" supabase/migrations/*.sql` ต้องยังเจอ | ✅ |

## R1-SHELL — เชลล์จอใหญ่ (แจ้งโดยผู้ใช้ 31 ส.ค. 2569)

| ID | Trigger | Role | สิ่งที่ต้องเห็น | สถานะ |
|----|---------|------|----------------|--------|
| R1-SHELL-01 | เปิดที่ ≥1024px | ทั้งสอง | ชื่อบริษัทบน sidebar เป็นตัวอักษรสว่าง อ่านออกบนพื้นกรมท่า (เดิมเป็นสีเกือบดำ มองไม่เห็น) | 👤 |
| R1-SHELL-02 | เปิดที่ ≥1024px | ทั้งสอง | ชื่อผู้ใช้ท้าย sidebar อ่านออก และมีปุ่ม **ออกจากระบบ** ใต้ชื่อ (เดิมไม่มีทางออกจากระบบบนจอใหญ่เลย) | 👤 |
| R1-SHELL-03 | เอาเมาส์ชี้ลิงก์เมนู | ทั้งสอง | พื้นหลังลิงก์เปลี่ยนสี (`bg-sidebar-hover` กลับมาทำงาน) | 👤 |
| R1-SHELL-04 | กดออกจากระบบทั้งจอเล็กและจอใหญ่ | ทั้งสอง | ทำงานเหมือนกันทั้งสองที่ (ตรรกะเดียวกันใน `SignOutButton`) | 👤 |
| R1-SHELL-05 | ตัวตรวจคลาสที่ไม่ถูกสร้าง | — | `npm run verify:tokens` ผ่าน · ลองลบ `--color-sidebar-title` ออกแล้วต้อง**แดง** | ✅ |

## R1-PICK — เลือกคนหลายคนพร้อมกัน (แจ้งโดยผู้ใช้ 31 ส.ค. 2569)

| ID | Trigger | Role | สิ่งที่ต้องเห็น | สถานะ |
|----|---------|------|----------------|--------|
| R1-PICK-01 | เปิด `/attendance` ที่ 390px | ทั้งสอง | ชื่อคนงานอ่านได้เต็มชื่อ ไม่ถูกบีบเหลือ "ค…" (เดิมช่องครึ่งวัน/OT/ปุ่ม กินความกว้างทั้งแถว) | 👤 |
| R1-PICK-02 | แตะที่แถวคนงาน | ทั้งสอง | ทั้งแถวติ๊กเลือกได้ · พื้นหลังเปลี่ยนเป็นสีแบรนด์อ่อน · ช่องติ๊กขึ้นเครื่องหมายถูก | 👤 |
| R1-PICK-03 | เลือกไว้ ≥1 คน | ทั้งสอง | แถบล่างเปลี่ยนเป็น "เลือกไว้ n คน" + ปุ่ม **ลงชื่อ n คน** | 👤 |
| R1-PICK-04 | กดลงชื่อทั้งชุด | ทั้งสอง | ทุกคนถูกลงชื่อ · toast สรุปจำนวน · รายชื่อย้ายไป "เข้าไซต์แล้ว" · ค่าแรงรวมอัปเดต | 👤 |
| R1-PICK-05 | ตัวเลือกครึ่งวัน / OT | owner | โผล่เป็นบรรทัดที่สองเฉพาะคนที่ติ๊กไว้ · ตั้งรายคนได้ก่อนกดบันทึกชุด | 👤 |
| R1-PICK-06 | คนที่เต็มวันที่ไซต์อื่นแล้ว | ทั้งสอง | ติ๊กไม่ได้ (ปุ่มแถว disabled) และปุ่ม "เลือกทุกคน" ไม่นับคนกลุ่มนี้ | 👤 |
| R1-PICK-07 | ปุ่ม "เลือกทุกคน" / "ล้างที่เลือก" | ทั้งสอง | สลับสถานะได้ถูกต้อง | 👤 |
| R1-PICK-08 | สคริปต์ตรวจเดิม | — | `data-day-wage` ยังอยู่ในผลเรนเดอร์ฝั่งเซิร์ฟเวอร์ (ตอนยังไม่มีใครถูกติ๊ก) — `verify-attendance.mjs` ยังหาเจอ | ✅ |

## R1-VIS — ความเรียบร้อยของจอ

| ID | Trigger | สิ่งที่ต้องเห็น | สถานะ |
|----|---------|----------------|--------|
| R1-VIS-01 | ทุกหน้าที่แก้ · 390/768/1440 · สว่าง+มืด | ไม่มีอะไรล้นขอบ/ถูกตัด (วัดขอบขวาต่ออิลิเมนต์ ตาม §17.7) | 👤 |
| R1-VIS-02 | ปุ่มลอย (บันทึก/แถบสรุป) บน iPhone มี safe-area | ไม่ทับแถบเมนูล่าง (`calc(4.5rem + env(safe-area-inset-bottom))`) | 👤 |
| R1-VIS-03 | แผ่นเลื่อนขึ้นทั้งสองแผ่น | เปิด-ปิดลื่น ไม่บังจนแตะปิดไม่ได้ · ปุ่มปิดชัดเจน | 👤 |

## R2-REPORT — หน้ารายงาน `/reports` (คำสั่งเจ้าของ 31 ส.ค. 2569)

> ฟังก์ชัน `report_*` ถูก apply เข้าฐานข้อมูลจริงแล้วผ่าน MCP · `get_advisors` (security)
> **ไม่มีคำเตือนใหม่** — ทั้ง 6 ตัวเป็น `security invoker` จึงไม่เข้าเงื่อนไขของ linter

| ID | Trigger | Role | สิ่งที่ต้องเห็น | สถานะ |
|----|---------|------|----------------|--------|
| R2-REPORT-01 | หัวหน้าไซต์เปิด `/reports` | supervisor | ถูกเด้งกลับหน้าแรก (guard อยู่ใน `layout.tsx` ไม่ใช่ page — รหัสสถานะจึงถูกต้อง) | 👤 |
| R2-REPORT-02 | หัวหน้าไซต์เรียก RPC ตรง ๆ | supervisor | ได้ **0 แถว** ไม่ใช่เลข 0 (`is_owner()` ใน where) — `select * from report_summary(...)` | 👤 |
| R2-REPORT-03 | เปิดเดือนที่มีข้อมูล | owner | แถบสรุป 3 ช่อง + % เทียบเดือนก่อน · กราฟรายวัน · หมวด · ไซต์ · คนงาน ครบ | 👤 |
| R2-REPORT-04 | สลับเป็น "รายปี" | owner | กราฟเปลี่ยนเป็นรายเดือน 12 แท่ง · ป้ายเทียบเป็น "ปีก่อน" | 👤 |
| R2-REPORT-05 | ยอดในหน้ารายงาน vs หน้าไซต์ | owner | ต้นทุน/รายรับของไซต์เดียวกันในช่วงเดียวกัน **ต้องตรงกัน** (ใช้สูตรเดียวกัน) | 👤 |
| R2-REPORT-06 | ผลรวมของ "ต้นทุนแยกหมวด" | owner | เท่ากับ "ต้นทุนที่เกิดขึ้น" พอดี เพราะมีแถวค่าแรงรวมอยู่ด้วย | 👤 |
| R2-REPORT-07 | เบิกล่วงหน้า / ปิดรอบจ่าย | owner | อยู่ในช่อง "เงินสดออก" แยกต่างหาก · **ไม่ถูกบวกเข้าต้นทุน** (§5.4) | 👤 |
| R2-REPORT-08 | เดือนที่ยังไม่มีรายการ | owner | สถานะว่างพร้อมปุ่ม "บันทึกรายการ" ไม่ใช่กราฟเปล่า ๆ | 👤 |
| R2-REPORT-09 | ช่วงที่มีรายการรออนุมัติ | owner | แถบเตือนว่าตัวเลขนับเฉพาะที่อนุมัติแล้ว + ลิงก์ไปคิวอนุมัติ | 👤 |
| R2-REPORT-10 | กรองไซต์ | owner | ทุกส่วนเปลี่ยนตาม · ตาราง "แยกตามไซต์" หายไป (เหลือไซต์เดียวย่อมไม่มีอะไรให้เทียบ) | 👤 |
| R2-REPORT-11 | จอ 390px | owner | กราฟไม่ล้นขอบ · ป้ายวันโชว์ทุก 5 วันไม่ทับกัน · ชื่อหมวด/คนอ่านออกเต็ม | 👤 |
| R2-REPORT-12 | กราฟกับคนตาบอดสี | owner | แยกเข้า/ออกได้จาก**ตำแหน่งขึ้น-ลง** ไม่ต้องพึ่งเขียว-แดง + มีคำอธิบายสี | 👤 |
| R2-REPORT-13 | ปี พ.ศ. | owner | ป้ายทุกที่เป็น พ.ศ. แต่ค่าใน URL/ฐานข้อมูลเป็น ค.ศ. (`?p=2026-08`) | 👤 |
| R2-REPORT-14 | รวมยอดในฐานข้อมูล | — | หน้านี้ไม่มี `.filter().reduce()` บนแถวดิบ — ทุกยอดมาจาก RPC (โค้ด) | ✅ |
| R2-REPORT-15 | ไม่มี JS ฝั่ง client | — | ทั้งหน้าเป็น Server Component · กราฟเป็น SVG ล้วน ไม่เพิ่ม dependency | ✅ |
