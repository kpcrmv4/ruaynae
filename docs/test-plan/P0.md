# P0 · ฐานระบบ — ตารางตรวจรับ

> เขียน**ก่อน**โค้ด ตาม `kp-acceptance-test-matrix`
> `tsc` เขียวและ `next build` เขียว **ไม่นับ**เป็นหลักฐานของแถวใดในนี้ — ทั้งสองไม่ได้ตรวจสักชั้นใน 4 ชั้น
> ห้ามติ๊ก ✅ สิ่งที่พิสูจน์ไม่ได้ · พิสูจน์ยังไม่ได้ = `⚠️ blocked` พร้อมเหตุผล

**สถานะ:** `☐` ยังไม่ตรวจ · `✅` ผ่านโดยมีคำสั่งรันได้รองรับ · `❌` ตก · `⚠️` blocked (มีเหตุผล) · `👤` ต้องคนตรวจ

---

## 0 · สำรวจพื้นผิว (นับก่อน แล้วค่อยเขียนแถว)

| นับอะไร | จากไหน | จำนวน |
|---|---|---|
| Routes (หน้า) | `/`, `/login` | **2** |
| API endpoints × method | `POST /api/auth/pin`, `POST /api/auth/login`, `POST /api/auth/logout`, `POST /api/auth/demo` | **4** |
| Roles | `anon`, `owner`, `site_supervisor` | **3** |
| ปุ่ม/ช่องกรอกบน `/login` | แป้นตัวเลข 10 + ปุ่มลบ 1 + แท็บ 2 + อีเมล 1 + รหัสผ่าน 1 + ปุ่มเข้าสู่ระบบ 1 + ปุ่มเดโม่ 1 | **17** |
| ปุ่ม/ลิงก์บน shell | ลิงก์ sidebar 8 + bottom nav 5 + สลับธีม 1 + ออกจากระบบ 1 | **15** |
| ตัวเลือก (enum) ที่มีผลใน P0 | `user_role` 2 ค่า · ธีม 3 ค่า (light/dark/system) | **5** |
| ตาราง | `profiles`, `audit_log`, `login_attempts` | **3** |
| ฟังก์ชัน/ทริกเกอร์ | `is_owner()`, `audit_row()` trigger | **2** |
| RLS policies | `profiles` 4 · `audit_log` 1 · `login_attempts` 1 | **6** |
| **พื้นผิวที่ต้องมีคนเขียนลงไป** | คอลัมน์ของ `profiles` ที่ไม่ใช่ generated: `full_name`, `role`, `pin_hash`, `is_active` | **4** |

**อัตราส่วนที่ต้องปิดก่อนจบเฟส:** routes 2/2 · endpoints 4/4 · roles 3/3 · ปุ่ม 32/32 · enum 5/5 · ตาราง 3/3 · policy 6/6 · คอลัมน์ที่ต้องมีคนเขียน 4/4

---

## P0-INF — โครงสร้างและ gate (elements 5/5)

| ID | Trigger | Role | UI | UX | API | DB | สถานะ |
|----|---------|------|----|----|-----|----|--------|
| P0-INF-01 | `npm run typecheck` บน repo ที่ลบ `.next` ทิ้งแล้ว | n/a | n/a | n/a | n/a — คำสั่ง exit `0` | n/a | ✅ |
| P0-INF-02 | `npm run verify:contrast` | n/a | n/a | n/a | stdout ลงท้าย `0 below 4.5:1` และมีบรรทัด `58 pairs checked` | n/a | ✅ |
| P0-INF-03 | ตัวตรวจคอนทราสต์ต้อง**แดงเป็น** — แก้ `--muted` เป็น `#999999` ชั่วคราว | n/a | n/a | n/a | exit ≠ 0 และมีคำว่า `FAIL` ใน stdout | n/a | ✅ |
| P0-INF-04 | ค่าโหมดสว่างกับมืดต้องไม่เท่ากัน (กันบั๊กที่ `.dark` ถูกอ่านเป็น `:root`) | n/a | n/a | n/a | รายงาน LIGHT กับ DARK ต้องมี **≥ 5 บรรทัดที่ตัวเลขต่างกัน** | n/a | ✅ |
| P0-INF-05 | `vercel.json` | n/a | n/a | n/a | `regions` = `["sin1"]` เป๊ะ | n/a | ✅ |
| P0-INF-06 | `next.config.ts` | n/a | n/a | n/a | `allowedDevOrigins` มี `localhost`, `127.0.0.1`, `*.localhost` ครบ 3 · header ของ `/sw.js` มี `Cache-Control: no-cache, no-store, must-revalidate` | n/a | ✅ |
| P0-INF-07 | `.env.local` ต้องไม่อยู่ใน git | n/a | n/a | n/a | `git ls-files` ไม่มีบรรทัด `.env.local` (นับได้ **0**) | n/a | ✅ |

> P0-INF-03 คือ red-test ของ P0-INF-02 — ตัวตรวจที่แดงไม่เป็นยังไม่นับเป็นตัวตรวจ

---

## P0-DB — schema · RLS · audit (ตาราง 3/3 · policy 6/6 · คอลัมน์ 4/4)

| ID | Trigger | Role | UI | UX | API | DB | สถานะ |
|----|---------|------|----|----|-----|----|--------|
| P0-DB-01 | ใช้ secret key สร้างแถว `profiles` (owner) | service | n/a | n/a | n/a | `profiles` **+1** แถว `role='owner'`, `is_active=true` · `audit_log` **+1** แถว `action='INSERT'`, `table_name='profiles'`, `before IS NULL`, `after->>'role'='owner'` | ✅ |
| P0-DB-02 | แก้ `full_name` ของตัวเอง | owner | n/a | n/a | n/a | `profiles.full_name` เปลี่ยน · `audit_log` **+1** แถว `action='UPDATE'` ที่ `before->>'full_name' <> after->>'full_name'` | ✅ |
| P0-DB-03 | supervisor พยายามแก้ `role` ของตัวเองเป็น `owner` | site_supervisor | n/a | n/a | n/a | error ตรงกับ `/ROLE_CHANGE_FORBIDDEN/` · `profiles.role` **ยังเป็น `site_supervisor`** · `audit_log` **ไม่เพิ่ม** | ✅ |
| P0-DB-04 | supervisor อ่านแถว `profiles` ของ owner | site_supervisor | n/a | n/a | n/a | คืน **0 แถว** *และ* อ่านแถวของตัวเองได้ **1 แถว** ในคำสั่งเดียวกัน (กัน `0===0`) | ✅ |
| P0-DB-05 | owner อ่าน `profiles` ทั้งหมด | owner | n/a | n/a | n/a | คืนจำนวนแถว = จำนวนที่ seed สร้าง (**3**) | ✅ |
| P0-DB-06 | supervisor อ่าน `audit_log` | site_supervisor | n/a | n/a | n/a | คืน **0 แถว** *และ* owner อ่านคำสั่งเดียวกันได้ **> 0 แถว** | ✅ |
| P0-DB-07 | UPDATE บน `audit_log` | owner | n/a | n/a | n/a | ⚠️ **ไม่มี error** — ไม่มี policy สำหรับ action นี้ PostgREST จึงตอบว่าสำเร็จ แต่โดน **0 แถว** · วัดที่จำนวนแถวที่ถูกกระทบ (= 0) และ `count(audit_log)` เท่าเดิม และไม่มีแถวที่ถูกแก้ค่า | ✅ |
| P0-DB-08 | DELETE บน `audit_log` | owner | n/a | n/a | n/a | ⚠️ **ไม่มี error** — ไม่มี policy สำหรับ action นี้ PostgREST จึงตอบว่าสำเร็จ แต่โดน **0 แถว** · วัดที่จำนวนแถวที่ถูกกระทบ (= 0) และ `count(audit_log)` เท่าเดิม และไม่มีแถวที่ถูกแก้ค่า | ✅ |
| P0-DB-09 | `anon` อ่าน `profiles` | anon | n/a | n/a | n/a | คืน **0 แถว** *และ* คำสั่งเดียวกันด้วย owner คืน **3 แถว** | ✅ |
| P0-DB-10 | `pin_hash` ต้องไม่ซ้ำ — สร้างผู้ใช้ 2 คนด้วย PIN เดียวกัน | service | n/a | n/a | n/a | คนที่สอง error ตรงกับ `/duplicate key or profiles_pin_hash_key/` · `profiles` **+1 แถวเท่านั้น** | ✅ |
| P0-DB-11 | `is_owner()` ต้องไม่มีทางลัด service-role | n/a | n/a | n/a | ซอร์สของฟังก์ชัน (ตัดคอมเมนต์ออกก่อน) ต้อง **ไม่มี** `auth.uid() is null` | n/a | ✅ |
| P0-DB-12 | ทุกตารางต้องเปิด RLS | n/a | n/a | n/a | n/a | `pg_tables` ใน schema `public` ที่ `rowsecurity=false` ต้องมี **0 แถว** | ✅ |
| P0-DB-13 | ทุกคอลัมน์ที่ไม่ใช่ generated ต้องมีโค้ดเขียนลงไป | n/a | n/a | n/a | `full_name`/`role`/`pin_hash`/`is_active` ปรากฏใน payload ของ `.insert(` หรือ `.update(` อย่างน้อยที่ละ 1 ที่ | n/a | ☐ |
| P0-DB-15 | `pin_hash` ที่เก็บไว้ต้องใช้เป็นรหัสผ่านไม่ได้ | service | n/a | n/a | n/a | เอาค่า `pin_hash` จากตารางไป signInWithPassword → error ตรงกับ `/Invalid login credentials/` *และ* `derivePassword(pin)` ของคนเดียวกันล็อกอินสำเร็จ (พิสูจน์ว่าสองค่ามาจากคนละ domain จริง ไม่ใช่แค่ต่างกันโดยบังเอิญ) | ☐ |
| P0-DB-14 | `get_advisors(security)` หลัง migration | n/a | n/a | n/a | จำนวน advisor ระดับ `ERROR` = **0** | n/a | ✅ |

| P0-DB-01b | audit ของ `profiles` | n/a | n/a | n/a | n/a | `before`/`after` ต้อง**ไม่มีคีย์ `pin_hash`** ในทุกแถว (นับได้ 0 จากทั้งหมด) — ไม่งั้น hash จะไปนอนในตารางที่ลบไม่ได้ | ✅ |
| P0-DB-17 | อ่าน `login_attempts` | site_supervisor | n/a | n/a | n/a | คืน **0 แถว** *และ* owner อ่านคำสั่งเดียวกันได้ **> 0** | ✅ |
| P0-DB-16 | `database.types.ts` หลัง migration | n/a | n/a | n/a | นับบล็อก `Row: {` ได้ **≥ 2** — ไฟล์ stub ที่มีแต่ enum ทำให้ tsc เขียวทั้งที่ data layer ไม่ถูกตรวจเลย | n/a | ✅ |

---

## P0-AUTH — ล็อกอิน (endpoints 4/4 · elements 17/17)

| ID | Trigger | Role | UI | UX | API | DB | สถานะ |
|----|---------|------|----|----|-----|----|--------|
| P0-AUTH-01 | กรอก `admin@demo.com` / `123456` แล้วกดเข้าสู่ระบบ | anon→owner | หลังเข้าเห็นหัวข้อ `ภาพรวม` | ปุ่ม `disabled` ระหว่างส่ง · URL เปลี่ยนเป็น `/` | `POST /api/auth/login` → **`200`** | มีคุกกี้ `sb-*-auth-token` ใน response · `profiles` ไม่เพิ่มแถว | ✅ |
| P0-AUTH-02 | รหัสผ่านผิด | anon | ข้อความใต้ฟอร์ม `อีเมลหรือรหัสผ่านไม่ถูกต้อง` | ไม่มี toast สำเร็จ · ปุ่มกลับ enabled | **`401`** `body.error='INVALID_CREDENTIALS'` | ไม่มีคุกกี้ session ถูกตั้ง | ✅ |
| P0-AUTH-03 | กด PIN `246810` ครบ 6 หลัก | anon→site_supervisor | หลังเข้าเห็นชื่อ `อนุชา พรมมา` ในแถบผู้ใช้ | จุด PIN เต็ม 6 จุดแล้วส่งอัตโนมัติ ไม่ต้องกดปุ่มยืนยัน | `POST /api/auth/pin` → **`200`** `body.role='site_supervisor'` | คุกกี้ session ถูกตั้ง**บน response object** (ไม่ใช่ผ่าน `cookies()` ของ `next/headers`) | ✅ |
| P0-AUTH-03b | ล็อกอินด้วย PIN สำเร็จ | site_supervisor | n/a | n/a | body ที่คืนกลับ **ไม่มี** `@staff.invalid` — อีเมลสังเคราะห์ห้ามโผล่ถึง client | n/a | ✅ |
| P0-AUTH-04 | PIN ผิด `000000` | anon | ข้อความ `PIN ไม่ถูกต้อง` · จุดถูกล้างกลับเป็น 0 | ไม่ redirect | **`401`** `body.error='INVALID_PIN'` | ไม่มีคุกกี้ session | ✅ |
| P0-AUTH-05 | ยิง `POST /api/auth/pin` ผิด 6 ครั้งติดจาก IP เดียว | anon | n/a | n/a | ครั้งที่ 6 → **`429`** `body.error='RATE_LIMITED'` · หากครั้งแรกได้ `429` อยู่แล้วให้รายงาน **`⏭ undecided`** ไม่ใช่ fail (หน้าต่างจากรอบก่อนยังไม่รีเซ็ต) | ไม่มีคุกกี้ session | ✅ |
| P0-AUTH-06 | PIN ยาวไม่ครบ 6 หลัก (`24681`) | anon | ปุ่มยืนยันไม่ทำงาน | n/a | **`400`** `body.error='PIN_LENGTH'` | ไม่มีคุกกี้ session | ✅ |
| P0-AUTH-07 | ผู้ใช้ที่ `is_active=false` ล็อกอิน | anon | ข้อความ `บัญชีนี้ถูกปิดใช้งาน` | ไม่ redirect | **`403`** `body.error='ACCOUNT_DISABLED'` | ไม่มีคุกกี้ session | ☐ |
| P0-AUTH-08 | กดออกจากระบบ | owner | กลับมาหน้า `/login` | n/a | `POST /api/auth/logout` → **`200`** | คุกกี้ session ถูกล้าง · เรียก `/` ต่อทันทีได้ `307` ไป `/login` | ✅ |
| P0-AUTH-09 | double-click ปุ่มเข้าสู่ระบบ | anon | ไปที่ `/` ครั้งเดียว | ปุ่ม `disabled` หลังคลิกแรก | คำขอที่สองไม่ถูกส่ง (นับ request = **1**) | ไม่มี session ซ้อน | 👤 (กันกดซ้ำอยู่ฝั่ง client — ต้องมีเบราว์เซอร์จริง รอ Playwright P8) |
| P0-AUTH-10 | ไม่ตั้ง `ENABLE_DEMO_LOGIN` เลย | anon | ปุ่ม**ไม่ปรากฏ** *และ* ปุ่ม `เข้าสู่ระบบ` ปรากฏอยู่บนหน้าเดียวกัน (ยึดกับสิ่งที่ต้องมี) | n/a | `POST /api/auth/demo` → **`404`** (ไม่ใช่ `403` — 403 ยืนยันว่า route มีอยู่) | ไม่มีคุกกี้ session | ✅ |
| P0-AUTH-11 | ตั้ง `ENABLE_DEMO_LOGIN=1` | anon | ปุ่มปรากฏ ข้อความ `เข้าใช้แบบเดโม่` | กดแล้วเข้าเป็น owner | `POST /api/auth/demo` → **`200`** | คุกกี้ session ถูกตั้ง | ✅ |
| P0-AUTH-12 | เปลี่ยนรหัสผ่านตัวเอง | owner | ฟอร์มมีช่อง `รหัสผ่านปัจจุบัน` | ส่งโดยไม่กรอกช่องนั้นไม่ได้ | ไม่ส่งรหัสปัจจุบัน → **`400`** `body.error='CURRENT_PASSWORD_REQUIRED'` | รหัสไม่เปลี่ยน | ☐ |

---

## P0-API — ขอบเขตของ endpoint (endpoints 4/4 × unauth + wrong-role)

| ID | Trigger | Role | UI | UX | API | DB | สถานะ |
|----|---------|------|----|----|-----|----|--------|
| P0-API-01 | `POST /api/auth/logout` โดยไม่มี session | anon | n/a | n/a | **`401` ไม่ใช่ `307`** — ต้องไม่ถูก redirect ไปหน้า HTML | ไม่มีอะไรเปลี่ยน | ✅ |
| P0-API-02 | `GET /api/auth/pin` (method ที่ไม่รองรับ) | anon | n/a | n/a | **`405`** | ไม่มีอะไรเปลี่ยน | ✅ |
| P0-API-03 | เรียก endpoint ที่**มีอยู่จริง** ใน `/api/*` ตอนไม่ล็อกอิน (เช่น `POST /api/auth/logout`) | anon | n/a | n/a | `content-type` ขึ้นต้นด้วย `application/json` และ status **`401`** — ห้าม `307` และห้าม `text/html` (path ที่ไม่มีอยู่จริงจะได้หน้า 404 HTML ของ Next เสมอ จึงใช้ทดสอบข้อนี้ไม่ได้) | n/a | ✅ |
| P0-API-04 | `GET /sw.js` ตอนไม่ล็อกอิน | anon | n/a | n/a | **`200`** `content-type` เป็น JavaScript — ห้าม `307` ไป `/login` (ไม่งั้นติดตั้ง PWA ไม่ได้และ push ตายเงียบ) | n/a | ✅ |
| P0-API-05 | `GET /manifest.webmanifest` ตอนไม่ล็อกอิน | anon | n/a | n/a | **`200`** ไม่ใช่ `307` | n/a | ✅ |
| P0-API-06 | `GET /` ตอนไม่ล็อกอิน | anon | n/a | n/a | **`307`** → `location` ลงท้ายด้วย `/login` | n/a | ✅ |
| P0-API-07 | `GET /login` ตอนล็อกอินแล้ว | owner | n/a | n/a | **`307`** → `/` (ห้ามวนกลับ `/login` ซ้ำ) | n/a | ✅ |

---

## P0-UI — shell และธีม (elements 15/15 · ตัวเลือกธีม 3/3)

| ID | Trigger | Role | UI | UX | API | DB | สถานะ |
|----|---------|------|----|----|-----|----|--------|
| P0-UI-01 | เปิด `/` ที่ความกว้าง 1440 | owner | `nav.sidebar` แสดง · ลิงก์ในนั้นนับได้ **8** | รายการที่ตรงกับ URL ปัจจุบันมีคลาส active | n/a | n/a | ✅ |
| P0-UI-02 | เปิด `/` ที่ความกว้าง 390 | owner | `nav.sidebar` `display:none` · `nav.bottomnav` แสดง · ช่องนับได้ **5** | ช่องกลางไม่มีข้อความกำกับ (`textContent.trim()` = `''`) และมี `aria-label` ไม่ว่าง | n/a | n/a | 👤 (ครึ่งที่วัดจาก HTML ได้ผ่านที่ P0-UI-02a · `display:none` ตาม media query ต้องมี viewport จริง) |
| P0-UI-03 | เปิด `/` ที่ 390 | site_supervisor | ช่องที่ 5 มีข้อความ `เพิ่มเติม` | กดแล้ว bottom sheet เปิด (`aria-expanded="true"`) | n/a | n/a | 👤 (ครึ่งที่วัดจาก HTML ได้ผ่านที่ P0-UI-03a · `aria-expanded` หลังกดต้องมีการโต้ตอบจริง) |
| P0-UI-02a | เปิด `/` (วัดจาก HTML ที่เซิร์ฟเวอร์เรนเดอร์) | owner | `nav[data-nav=bottom]` มีช่องนับได้ **5** | ช่องกลางมี `aria-label` ไม่ว่าง (`บันทึกรายจ่าย`) | n/a | n/a | ✅ |
| P0-UI-03a | เปิด `/` (วัดจาก HTML) | owner **และ** site_supervisor | ช่องที่ 5 มีข้อความ `เพิ่มเติม` **ทั้งสอง role** | n/a | n/a | n/a | ✅ |
| P0-UI-04 | supervisor เปิด `/` | site_supervisor | ลิงก์ `รออนุมัติ` และ `ประวัติการแก้ไข` **ไม่ปรากฏ** *และ* ลิงก์ `ภาพรวม` ปรากฏบนหน้าเดียวกัน | n/a | n/a | n/a | ✅ |
| P0-UI-04b | หัวหน้าไซต์เปิด `/` | site_supervisor | HTML ที่เซิร์ฟเวอร์ส่งออกมา**ไม่มี** `รออนุมัติ` และ `ประวัติการแก้ไข` *และ***มี** `คนเข้าไซต์` + `bg-sidebar` บนหน้าเดียวกัน (ยึด landmark ฝั่งบวก ไม่งั้น "ไม่เจอ" อาจแปลว่าหน้าไม่ได้เรนเดอร์เลย) | n/a | n/a | n/a | ✅ |
| P0-UI-05 | กดสลับธีมเป็นมืด | owner | `<html>` มีคลาส `dark` | `getComputedStyle(body).backgroundColor` = `rgb(11, 16, 23)` | n/a | ค่าถูกจำใน localStorage คีย์ `theme` = `dark` | ☐ |
| P0-UI-06 | กดสลับธีมเป็นสว่าง | owner | `<html>` ไม่มีคลาส `dark` | `backgroundColor` = `rgb(242, 245, 250)` | n/a | localStorage `theme` = `light` | ☐ |
| P0-UI-07 | ธีม `system` | owner | คลาสตาม `prefers-color-scheme` ของเครื่อง | ไม่มีการกระพริบสีตอนโหลด (`suppressHydrationWarning` อยู่บน `<html>`) | n/a | localStorage `theme` = `system` | 👤 |
| P0-UI-08 | ฟอนต์ไทย | any | `document.fonts.check('600 20px "IBM Plex Sans Thai"')` = `true` | n/a | n/a | n/a | ☐ |
| P0-UI-09 | ไม่มี emoji ใน UI | n/a | grep หา emoji ในไฟล์ `src/**/*.tsx` (ตัดคอมเมนต์ก่อน) ได้ **0 รายการ** | n/a | n/a | n/a | ☐ |
| P0-UI-10 | ไม่มี `alert()` ใน UI | n/a | grep `alert(` ใน `src/**/*.tsx` (ตัดคอมเมนต์ก่อน) ได้ **0 รายการ** | n/a | n/a | n/a | ☐ |
| P0-UI-11 | หน้าไม่เลื่อนออกด้านข้าง | owner | ที่ 390px: `document.documentElement.scrollWidth <= innerWidth` | n/a | n/a | n/a | 👤 (`scrollWidth <= innerWidth` ต้องมี viewport จริง รอ Playwright P8) |
| P0-UI-12 | ปุ่มทุกปุ่มกดติด (hydration ทำงาน) | owner | กดสลับธีมแล้วคลาสเปลี่ยนจริงภายใน 500ms | ไม่มี error ใน console (`list_console_messages` ระดับ error = **0**) | n/a | n/a | ☐ |
| P0-UI-13 | ไล่เปิดทุก `href` ที่อยู่ในเมนู (sidebar + แถบล่าง + ปุ่มกลาง) | owner | ทุกเส้นทางตอบ **200** — ลิงก์ที่ปลายทาง 404 คือปุ่มที่ไม่มีใครทดสอบ และไม่มีอะไรใน build จับได้ | n/a | n/a | n/a | ✅ |

> **แถวชั้น UI ที่ยืนยันด้วย Chrome MCP รอบนี้** (มีค่าที่วัดได้จริง แต่ยังไม่ได้เขียนเป็นสคริปต์ที่รันซ้ำได้):
> P0-AUTH-03 ชั้น UI — กด 5 หลักยังอยู่ `/login` และจุดขึ้น 5 จุด · หลักที่ 6 ส่งเองแล้ว `location.pathname` เปลี่ยนเป็น `/`
> · แป้นตัวเลขได้ `grid-template-columns` 3 คอลัมน์เท่ากัน · `.keypad-key` `border-width: 1px`
> **ยังไม่ติ๊ก** จนกว่าจะมี Playwright spec ใน P8 — ติ๊กจากสิ่งที่รันซ้ำไม่ได้คือติ๊กจากความจำ

---

## P0-SEC — ความปลอดภัยระดับตั้งค่า

| ID | Trigger | Role | UI | UX | API | DB | สถานะ |
|----|---------|------|----|----|-----|----|--------|
| P0-SEC-01 | ตั้งค่า Supabase Auth | n/a | n/a | n/a | `GET /auth/v1/settings` → `disable_signup` = `true` | n/a | ✅ |
| P0-SEC-02 | anonymous sign-in ปิด | n/a | n/a | n/a | `external.anonymous_users` = `false` | n/a | ✅ |
| P0-SEC-03 | secret key ต้องไม่หลุดฝั่ง client | n/a | grep `SUPABASE_SECRET_KEY` ในทุกไฟล์ใต้ `src/app/**` ที่มี `'use client'` ได้ **0 รายการ** | n/a | n/a | n/a | ☐ |
| P0-SEC-04 | ไม่มีตัวแปรลับขึ้นต้นด้วย `NEXT_PUBLIC_` | n/a | n/a | n/a | ชื่อคีย์ใน `.env.local` ที่ขึ้นต้น `NEXT_PUBLIC_` และมีคำว่า `SECRET`/`KEY`(ที่ไม่ใช่ `PUBLISHABLE`/`VAPID_PUBLIC`) = **0** | n/a | ✅ |
| P0-SEC-05 | ทุก Supabase call ต้อง destructure `error` | n/a | ทุกไฟล์ใน `src/**` ที่เรียก `.from(` แล้วไม่มี `error` ใน destructuring = **0 ไฟล์** | n/a | n/a | n/a | ☐ |

---

## สรุปความครอบคลุม (ต้องปิดก่อนเซ็นจบเฟส)

```
รวม 58 แถว                          (อัปเดต 30 ส.ค. 2569 หลัง scripts/verify-matrix.mjs จับติ๊กที่ไม่มีของรองรับ)
  ✅ ผ่านโดยมีคำสั่งรันได้รองรับ : 49  ← INF 7 · SEC 3 · DB 16 · AUTH 10 · API 7 · UI 6
  ☐  ยังไม่ตรวจ                  : 4
  👤 ต้องคนตรวจ / รอ Playwright P8 : 5
  ❌ ตก                          : 0
  ⚠️ blocked                     : 0
```

> **ถอนติ๊ก 4 แถวในรอบนี้** — `scripts/verify-matrix.mjs` พบว่า P0-AUTH-09,
> P0-UI-02, P0-UI-03, P0-UI-11 ถูกติ๊กไว้จากการวัดด้วย Chrome MCP ระหว่างเฟส P0
> ซึ่งไม่มีคำสั่งไหนรันซ้ำแล้วแดงได้ · ทั้งสี่แถวต้องการเบราว์เซอร์จริง
> (media query · viewport · การโต้ตอบ) จึงถูกลดเป็น 👤 รอ Playwright ที่ P8
> ส่วนที่ HTML ตัดสินได้ถูกดึงกลับมาเป็นของจริงที่ P0-UI-02a และ P0-UI-03a
> และ P0-DB-08 ได้ check จริงแล้วใน `verify-rls.mjs`

**หลักฐาน:** `node scripts/verify-p0.mjs` (19) · `verify-rls.mjs` (12) · `verify-auth.mjs` (20) · `verify-shell.mjs` (3)
กระทบยอดอัตโนมัติด้วย `node scripts/verify-matrix.mjs` — ทุก ✅ ต้องมี ID อยู่ใน label ของ check ที่รันได้

> **P0-UI-08 (ฟอนต์ไทย) เคยถูกติ๊กแล้วถอนกลับ** — ผมยืนยันด้วยตาในเบราว์เซอร์จริง
> แต่ยังไม่มีคำสั่งที่รันซ้ำได้ผูกอยู่ ตามกฎของสกิลจึงยังไม่นับเป็น ✅
> จะติ๊กได้เมื่อมี browser probe ใน P0-11

| อัตราส่วน | สถานะ |
|---|---|
| routes 2/2 | ☐ (`/login` ยังไม่สร้าง) |
| endpoints 4/4 มีแถว unauth + wrong-role | ☐ |
| roles 3/3 ปรากฏในแถวที่การมองเห็นต่างกัน | ☐ |
| ปุ่ม/ช่องกรอก 32/32 | ☐ |
| enum 5/5 (user_role 2 · theme 3) | ☐ |
| ตาราง 3/3 · policy 6/6 | ☐ |
| คอลัมน์ที่ต้องมีคนเขียน 4/4 | ☐ |

**กฎการติ๊ก:** แถวที่ ID มี `-DB-` `-SEC-` `-INF-` `-API-` ต้องมี ID ปรากฏอยู่ใน label ของ check
ที่รันได้จริงในสคริปต์ (`scripts/**/*.mjs`) — ติ๊กจากความจำไม่นับ และตอนจบเฟสต้องกระทบยอดสองทาง:
แถวที่ติ๊กแล้วไม่มี check → ✅ ปลอม · check ที่อ้าง ID ที่ไม่มีในตาราง → ตารางถูกแก้แล้วข้อกำหนดหายไปเงียบ ๆ
