# P9 · ตัวเชื่อม MCP อ่านอย่างเดียว — ตารางตรวจรับ

> ⚠️ **หมดอายุบางส่วนแล้ว (1 ก.ย. 2569)** — R6 เพิ่มเครื่องมือฝั่ง**เขียน** เข้ามา
> ตัวเชื่อมนี้จึงไม่ใช่ "อ่านอย่างเดียว" อีกต่อไป · แถวในไฟล์นี้ยังใช้ได้ทั้งหมด
> แต่ต้องอ่านว่าเป็นข้อกำหนดของ **7 tool ฝั่งอ่าน** เท่านั้น (P9-PROTO-07 นับรวมของใหม่ด้วย)
> · ฝั่งเขียนอยู่ที่ [`R6-mcp-write.md`](R6-mcp-write.md)

> เขียน**ก่อน**โค้ด ตาม `kp-acceptance-test-matrix`
> `tsc` เขียวและ `next build` เขียว **ไม่นับ**เป็นหลักฐานของแถวใดในนี้
> อ้างอิงสเปก `docs/superpowers/specs/2026-08-31-mcp-analytics-connector-design.md` §11

**สถานะ:** `☐` ยังไม่ตรวจ · `✅` ผ่านโดยมีคำสั่งรันได้รองรับ · `❌` ตก · `👤` ต้องคนตรวจ (เครื่องจริง/สายตา) · `⚠️` ปิดในเฟสนี้ไม่ได้ พร้อมเหตุผลกำกับในแถว

> 🔴 **ตัวเชื่อมนี้พังแบบที่ไม่มี error ให้เห็น** — header `WWW-Authenticate` ที่ใส่เพิ่มเพราะดู
> "ถูกต้องตามสเปก" จะทำให้ Claude เริ่ม OAuth discovery แล้วค้างหลังปุ่ม Connect ตลอดไปโดยไม่มี
> ข้อความ error ที่ไหนเลย · เรียก RPC เงินเดิมตรง ๆ ด้วย `service_role` จะได้ `null` ทุกคอลัมน์เงียบ ๆ
> เพราะ `auth.uid()` เป็น null แต่ AI จะไม่ฟ้อง แค่ตอบคำถามผิดด้วยความมั่นใจ · `resources/*` ที่ตอบ
> `-32601` แทน `[]` ทำให้แอป Claude โชว์ข้อความแดงทั้งที่เราไม่เคยประกาศ capability นั้น · ทุกแถว
> ในตารางนี้จึงเน้นตรวจสิ่งที่ **ดูเหมือนทำงานแต่ตอบผิด** ไม่ใช่สิ่งที่พังจนเห็นชัดจากหน้าจอ

---

## 0 · สำรวจพื้นผิว

| นับอะไร | จากไหน | จำนวน |
|---|---|---|
| ตารางใหม่ | `mcp_keys` · `mcp_call_log` | **2** |
| ฟังก์ชันฐานข้อมูล (ใหม่ 6 + แก้ไข 1) | `mcp_overview` · `mcp_sites` · `mcp_site_detail` · `mcp_transactions` · `mcp_pending` · `mcp_payroll` (ใหม่) · `audit_row()` (แก้ไขให้ตัด `key_hash` เพิ่ม) | **7** |
| MCP endpoint × method | `POST`+`GET /api/mcp/[key]` · `POST`+`GET /api/mcp` (Bearer) | **4** |
| endpoint จัดการคีย์ | `POST /api/settings/mcp-keys` · `DELETE /api/settings/mcp-keys/[id]` | **2** |
| Roles | `anon` · `owner` · `site_supervisor` | **3** |
| เครื่องมือ (tools) ที่ AI เรียกได้ | `get_metric_definitions` · `get_company_overview` · `list_sites` · `get_site_detail` · `search_transactions` · `get_pending_approvals` · `get_payroll_summary` | **7** |
| ปุ่มที่กดได้บน `/mcp` | สร้างคีย์ · คัดลอก URL · เพิกถอน | **3** |

**อัตราส่วนที่ต้องปิด:** ตาราง 2/2 · ฟังก์ชัน 7/7 · endpoint MCP 4/4 · endpoint จัดการคีย์ 2/2 · roles 3/3 · tools 7/7 · ปุ่ม 3/3

---

## P9-DB — ตาราง, RLS, audit

| ID | Trigger | Role | UI | UX | API | DB | สถานะ |
|----|---------|------|----|----|-----|----|--------|
| P9-DB-01 | อ่านตาราง `mcp_keys` | site_supervisor | n/a | n/a | n/a | **0 แถว** — ไม่มี policy ให้ role นี้อ่านเลย | ✅ `verify-mcp` |
| P9-DB-02 | อ่านตาราง `mcp_keys` | anon | n/a | n/a | n/a | **0 แถว** | ✅ `verify-mcp` |
| P9-DB-03 | insert `mcp_keys` สองแถวด้วย `key_hash` เดียวกัน | owner | n/a | n/a | n/a | แถวที่สอง **ล้มเหลว** — unique violation (`23505`) และไม่มีแถวใหม่เพิ่ม | ✅ `verify-mcp` |
| P9-DB-04 | แก้ `profiles.pin_hash` 1 แถว และแก้ `mcp_keys.key_hash` 1 แถว แล้วอ่าน `audit_log` ของสองแถวนั้น | owner | n/a | n/a | n/a | คอลัมน์ `before`/`after` ของทั้งสองแถว **ไม่มีทั้ง `pin_hash` และ `key_hash`** ปรากฏอยู่เลย | ✅ `verify-mcp` |
| P9-DB-05 | insert แล้ว update แล้ว revoke แถวใน `mcp_keys` (3 การกระทำ) | owner | n/a | n/a | n/a | `audit_log` ที่ `table_name = 'mcp_keys'` **เพิ่มขึ้น 3 แถว** — audit trigger ติดจริง | ✅ `verify-mcp` |
| P9-DB-06 | insert แถว `mcp_call_log` ของคีย์จริงก่อน แล้ว `UPDATE`/`DELETE` ตรงบนแถวนั้น | owner | n/a | n/a | n/a | ล้มเหลวหรือกระทบ **0 แถว** และแถวนั้น **ยังอยู่ครบ 1 แถว** หลังพยายาม — ไม่มี policy ให้แก้/ลบกับ role ใดเลยแม้แต่เจ้าของ | ✅ `verify-mcp` — **แก้แล้ว 31 ส.ค. 2569:** เดิมตรวจตอนตารางว่างเปล่าสำหรับคีย์นั้น (`upd===0 && del===0` เป็นเลขคณิตของตารางว่าง ไม่ใช่หลักฐาน — ถ้ามีคนเผลอเพิ่ม policy ให้แก้ได้วันหน้า แถวนี้ก็ยังเขียว) ตอนนี้แทรกแถวลง `mcp_call_log` เองก่อนด้วย service key แล้วเช็คทั้งผลของ `UPDATE`/`DELETE` และว่านับแถวได้ครบ 1 แถวจริงหลังพยายาม |

---

## P9-FN — ฟังก์ชัน `mcp_*`: สิทธิ์และความถูกต้องของตัวเลข

| ID | Trigger | Role | UI | UX | API | DB | สถานะ |
|----|---------|------|----|----|-----|----|--------|
| P9-FN-01 | เรียก `mcp_sites(...)` ตรง ๆ ด้วย SQL | anon | n/a | n/a | n/a | **permission denied for function** — ไม่มี `grant execute` ให้ `anon` บนฟังก์ชัน `mcp_*` ตัวใดเลย | ✅ `verify-mcp` |
| P9-FN-02 | เรียก `mcp_sites(...)` ตรง ๆ ด้วย SQL | authenticated | n/a | n/a | n/a | **permission denied** เช่นเดียวกัน — `grant` มีแค่ `service_role` | ✅ `verify-mcp` |
| P9-FN-03 | เรียก `mcp_sites(p_actor = เจ้าของ, ...)` เทียบกับ `site_money(site_id)` ที่เรียกด้วยสิทธิ์เจ้าของคนเดียวกัน ทุกไซต์ในระบบ | n/a | n/a | n/a | n/a | ทุกคอลัมน์เงิน (`cost_total`, `income_approved`, ฯลฯ) **เท่ากันถึงสตางค์ทุกไซต์** — เทียบเป็นสตริงหลัง cast `::numeric` ไม่ใช่ float | ✅ `verify-mcp` — เทียบด้วยสองคำขอ: เรียก `mcp_sites` ในทรานแซกชันสะอาด (ไม่มีใครสวมสิทธิ์ให้ก่อน เหมือน route จริงที่ใช้ service key) แล้วเอา JSON ที่ได้ไปเทียบกับ `site_money(null)` ในคำขอที่สองที่สวมสิทธิ์เจ้าของ · เทียบเป็น `numeric` ในฐานข้อมูล และ**นับ null แยกต่างหาก** เพราะ `null is distinct from null` เป็นเท็จ แถวจึงเขียวได้ทั้งที่ทั้งสองฝั่งว่าง · สคริปต์สร้างไซต์ตัวอย่างที่ทุกยอดต่างกันและไม่มีศูนย์ แล้วลบทิ้งใน `finally` — บนฐานว่างแถวนี้จะเป็น `0 === 0` ซึ่งไม่มีวันแดง · **พิสูจน์แล้วว่าแดงได้**: สลับให้อ่าน `cost_pending` มาเป็น `cost_wage` → ต่าง 1 · ให้อ่านคอลัมน์ที่ไม่มีอยู่ → null 2 ต่าง 2 |
| P9-FN-04 | เรียก `mcp_overview(p_actor = เจ้าของ, ...)` เทียบกับ `site_overview()` ด้วยวิธีสวมสิทธิ์เดียวกัน | n/a | n/a | n/a | n/a | ทุกคอลัมน์ **เท่ากันถึงสตางค์** ด้วยวิธีเทียบแบบเดียวกับ FN-03 | ✅ `verify-mcp` — วิธีเดียวกับ FN-03 และบังคับเพิ่มว่า `active_contract` / `active_income` / `active_cost` / `pending_total` ต้อง **มากกว่า 0** ไม่ใช่ null เท่ากับ null |
| P9-FN-05 | เรียก `mcp_overview(p_actor = id ของหัวหน้าไซต์, ...)` | site_supervisor (เป็น `p_actor`) | n/a | n/a | n/a | **raise exception `MCP_ACTOR_NOT_OWNER`** ไม่คืนแถวใด ๆ | ✅ `verify-mcp` |

---

## P9-PROTO — โปรโตคอล JSON-RPC และ HTTP

| ID | Trigger | Role | UI | UX | API | DB | สถานะ |
|----|---------|------|----|----|-----|----|--------|
| P9-PROTO-01 | 🔴 `POST /api/mcp/<คีย์ผิด>` method `ping` | n/a (คีย์ไม่ถูกต้อง) | n/a | n/a | **= 401** และ header `www-authenticate` **ไม่มี** (อ่านค่าได้ `null`) — header นี้ = สัญญาณเริ่ม OAuth discovery ที่ทำให้ connector ค้าง | n/a | ✅ `verify-mcp` |
| P9-PROTO-02 | `GET /api/mcp/<คีย์ถูกต้อง>` | owner (เจ้าของคีย์) | n/a | n/a | **= 405** และ header `Allow` มีค่าเป็น `POST` | n/a | ✅ `verify-mcp` |
| P9-PROTO-03 | `POST` JSON-RPC ที่ไม่มีฟิลด์ `id` (notification) | n/a | n/a | n/a | **= 202** และ `content-length` เป็น **`0`** (บอดี้ว่างจริง ไม่ใช่ `"{}"`) | n/a | ✅ `verify-mcp` |
| P9-PROTO-04 | `POST` method `resources/list` และ `resources/templates/list` | n/a | n/a | n/a | ทั้งคู่ตอบ `result` เป็น **`[]`** — ไม่มีคีย์ `error` และ **ไม่ใช่** โค้ด `-32601` | n/a | ✅ `verify-mcp` |
| P9-PROTO-05 | `POST` method `initialize` พร้อม `protocolVersion` เป็นค่าแปลก (เช่น `"9999-99-99"`) | n/a | n/a | n/a | ค่า `protocolVersion` ในคำตอบ **เท่ากับค่าที่ส่งไปทุกตัวอักษร** | n/a | ✅ `verify-mcp` |
| P9-PROTO-06 | `POST` อย่างน้อย 4 เมท็อด (`initialize` · `tools/list` · `tools/call` · `ping`) | n/a | n/a | n/a | **ไม่มี** header `mcp-session-id` ในคำตอบเมท็อดใดเลย | n/a | ✅ `verify-mcp` |
| P9-PROTO-07 | `POST` method `tools/list` | n/a | n/a | n/a | คืน tools **14 ตัว** พอดี (7 ฝั่งอ่านของ P9 + 7 ฝั่งเขียนของ R6) ทุกตัวมี `inputSchema` เป็น object ที่ไม่ใช่ `null`/`undefined` | n/a | ✅ `verify-mcp` — **แก้แล้ว 1 ก.ย. 2569:** เดิมบังคับ 7 ตัว · ตัวเลขนี้ต้องขยับด้วยมือทุกครั้งที่เพิ่ม tool โดยตั้งใจ |
| P9-PROTO-08 | `POST` method `ping` | n/a | n/a | n/a | **= 200** และ `result` เป็น `{}` | n/a | ✅ `verify-mcp` |
| P9-PROTO-09 | `POST` method `prompts/list` แล้วตามด้วย `prompts/get` ของแต่ละชื่อที่ได้มา | n/a | n/a | n/a | `prompts/list` คืน **3 รายการ** เป็นภาษาไทยล้วน · `prompts/get` ของทั้ง 3 ชื่อตอบ **200** ไม่มี error | n/a | ✅ `verify-mcp` |
| P9-PROTO-10 | `POST` method `initialize` | n/a | n/a | n/a | `result.instructions` มีคำว่า **`get_metric_definitions`** อยู่จริง · `result.capabilities` มีคีย์ `tools` และ `prompts` ทั้งคู่ | n/a | ✅ `verify-mcp` |

---

## P9-TOOL — เครื่องมือทั้ง 7 ตัว

| ID | Trigger | Role | UI | UX | API | DB | สถานะ |
|----|---------|------|----|----|-----|----|--------|
| P9-TOOL-01 | `tools/call` → `get_metric_definitions` (ไม่มีพารามิเตอร์) | n/a | n/a | n/a | **= 200** · `content[0].text` parse ได้ (JSON หรือข้อความ) · มีคำว่า **"ไม่นับซ้ำ"** อยู่ในเนื้อหา | n/a | ✅ `verify-mcp` — ⚠️ ข้อความจริงใน `METRIC_DEFINITIONS` ใช้คำว่า **“ห้ามนับซ้ำ”** ไม่ใช่ “ไม่นับซ้ำ” ที่เขียนไว้ตอนร่างแถวนี้ · ตัวตรวจยึดคำที่ส่งจริง และ**บังคับเพิ่ม**ว่าต้องมีสูตร `cost_total` และ `cost_wage` อยู่ในเนื้อหาด้วย (แน่นกว่าที่แถวขอตอนแรก) — แก้ข้อความคาดหวังให้ตรงกับของจริง 31 ส.ค. 2569 |
| P9-TOOL-02 | `tools/call` → `get_company_overview({})` | n/a | n/a | n/a | **= 200** · `content[0].text` **parse เป็น JSON ได้** โดยไม่ throw | n/a | ✅ `verify-mcp` |
| P9-TOOL-03 | `tools/call` → `list_sites({limit:1,offset:0})` แล้ว `list_sites({limit:1,offset:1})` | n/a | n/a | n/a | ทั้งคู่ **= 200** และคืนไซต์ **คนละ `id`** กัน — `offset` มีผลจริง | n/a | ✅ `verify-mcp` |
| P9-TOOL-04 | `tools/call` → `get_site_detail({site_id: <ไซต์จริง>})` | n/a | n/a | n/a | **= 200** · parse JSON ได้ · มีคีย์ `milestones` เป็น array · มีคีย์ `recent_transactions` เป็น array ที่มี **ไม่เกิน 10** รายการ แต่ละรายการมีฟิลด์ `txn_date` (วันที่) และ `amount` (ตัวเลข) ครบทุกแถว | n/a | ✅ `verify-mcp` |
| P9-TOOL-05 | `tools/call` → `search_transactions({limit:10})` | n/a | n/a | n/a | **= 200** · parse JSON ได้ · มีคีย์ **`total_count`** เป็นตัวเลข (ไม่ใช่สตริง) | n/a | ✅ `verify-mcp` |
| P9-TOOL-06 | `tools/call` → `get_pending_approvals({})` | n/a | n/a | n/a | **= 200** · parse JSON ได้ | n/a | ✅ `verify-mcp` |
| P9-TOOL-07 | `tools/call` → `get_payroll_summary({limit:50})` หลังใส่แถว `payroll_runs` ตัวอย่างไว้เอง | n/a | n/a | n/a | **= 200** · parse JSON ได้ · `balances` เป็น array · `runs` มีรอบจ่ายตัวอย่างที่ใส่ไว้เองพร้อม `site_name`/`total_accrued`/`total_advance_deducted`/`total_paid` ตรงเป๊ะ | n/a | ✅ `verify-mcp` — **แก้แล้ว 31 ส.ค. 2569:** เดิมเช็คแค่ `Array.isArray(j.runs)` ซึ่งจริงตลอดกาลเพราะ fixture ไม่เคยสร้างแถว `payroll_runs` เลย (คอลัมน์ `runs` ห่อด้วย `coalesce(...,'[]'::jsonb)` ใน `mcp_payroll()` — subquery พังทั้งก้อนก็ยังได้ `[]` ที่ผ่านเช็คเดิม) ตอนนี้สร้างรอบจ่ายปิดแล้ว 1 รอบผูกกับไซต์ตัวอย่าง แล้วยืนยันว่า `runs` มีรอบนั้นจริงพร้อมยอดที่ตรงกันทุกคอลัมน์ |
| P9-TOOL-08 | `tools/call` → `get_site_detail({site_id: <uuid สุ่มที่ไม่มีในตาราง>})` | n/a | n/a | n/a | **= 200** (ไม่ใช่ JSON-RPC error) และ `result.isError === true` พร้อมข้อความอธิบาย | n/a | ✅ `verify-mcp` — **แก้แล้ว 31 ส.ค. 2569:** `mcp_site_detail()` ยังไม่ raise เมื่อไม่พบไซต์ (มันคืน `{"site": null, …}`) แต่ `src/lib/mcp/execute.ts` เห็น `site == null` แล้วคืน `ok: false` พร้อมข้อความ *“ไม่พบไซต์ตาม site_id นี้ — ใช้ list_sites เพื่อดู site_id ที่ถูกต้อง”* ซึ่ง handler แปลงเป็น `isError: true` ใน 200 ตามเดิม · แก้ที่ชั้น TS ไม่ใช่ SQL เพราะไม่ต้องมี migration ใหม่ และเป็นชั้นเดียวกับที่แปลง `MCP_ACTOR_NOT_OWNER` อยู่แล้ว |
| P9-TOOL-09 | `tools/call` → `search_transactions({q: "ปูน,ทราย"})` เทียบกับ `search_transactions({q: "ทราย"})` | n/a | n/a | n/a | ผลลัพธ์ของคำค้นที่มีคอมมา **ไม่รวม** แถวที่มีแค่ "ทราย" อย่างเดียว — คอมมาไม่กลายเป็นเงื่อนไข OR ที่สองใน `.or()` | n/a | ✅ `verify-mcp` |

---

## P9-SEC — ความปลอดภัยและการอ่านอย่างเดียว

> ⚠️ ตั้งแต่ R6 แถว SEC-03/SEC-04 คุมเฉพาะ **7 tool ฝั่งอ่าน** — ของฝั่งเขียนมีแถวของตัวเอง
> ที่ `R6-SEC-*` และ `R6-TOOL-*` · การนับแถวก่อน/หลังยังเป็นหลักฐานเดียวที่เชื่อได้ว่า
> tool ฝั่งอ่านไม่ได้แอบเขียนอะไร

| ID | Trigger | Role | UI | UX | API | DB | สถานะ |
|----|---------|------|----|----|-----|----|--------|
| P9-SEC-01 | `tools/call` ด้วยคีย์ที่ตั้ง `revoked_at` แล้ว | n/a | n/a | n/a | **= 401** | n/a | ✅ `verify-mcp` |
| P9-SEC-02 | ยิง `tools/call` เกิน **60 ครั้ง/นาที** ด้วยคีย์เดียวกัน | n/a | n/a | n/a | ครั้งที่เกินเพดานได้ **= 200** และ `result.isError === true` พร้อมข้อความไทยที่บอกว่าเรียกถี่เกินไป — ตัวนับอยู่ใน `tools/call` เท่านั้น **ไม่ใช่ 429** ระดับ connector ที่เจ้าของอ่านว่า "ระบบเสีย" (หลักการเดียวกับ tool ที่ล้มเหลวต้องตอบ `isError` ไม่ใช่ JSON-RPC error) | n/a | ✅ `verify-mcp` |
| P9-SEC-03 | 🔴 ยิงครบทุก tool **ฝั่งอ่าน** (7 ตัว) แล้วรวมทุก response เป็นสตริงเดียว | n/a | n/a | n/a | **ไม่พบ** คำใดใน: `client_phone` · `address` · `object_key` · `thumb_key` · `pin_hash` · `key_hash` · `@staff.invalid` · `endpoint` · `p256dh` · `tax_id` | n/a | ✅ `verify-mcp` — ⚠️ **ตัด `address` ออกจากรายการสตริงที่ค้น** คำนี้สั้นจนชนข้อความที่ไม่มีพิษภัยได้ง่าย แล้วแถวจะแดงเรื่อย ๆ จนคนปิดมันทิ้ง · แทนที่ด้วยการยัด **ค่าจริง** ของ `address` และ `client_phone` ลงในไซต์ตัวอย่าง แล้วค้นหาค่านั้นในคำตอบทุกอัน ซึ่งจับการรั่วได้ตรงกว่าชื่อคอลัมน์ |
| P9-SEC-04 | 🔴 นับแถวทุกตารางที่มีอยู่ก่อน/หลังยิงครบทุก tool **ฝั่งอ่าน** (7 ตัว) | n/a | n/a | n/a | n/a | ทุกตาราง **เท่าเดิมทุกตาราง** ยกเว้น `mcp_call_log` ที่ต้อง **เพิ่มขึ้น** | ✅ `verify-mcp` |
| P9-SEC-05 | เรียก `tools/call` รวม N ครั้ง ทั้งที่สำเร็จและที่ทำให้เกิด error (เช่น P9-TOOL-08) | n/a | n/a | n/a | n/a | แถวใน `mcp_call_log` ของคีย์นั้น **เพิ่มขึ้นเท่ากับ N พอดี** ทั้งกรณี `ok=true` และ `ok=false` | ✅ `verify-mcp` |
| P9-SEC-06 | 🔴 `POST /api/mcp/<คีย์>` (method ใดก็ได้ เช่น `ping`) ตอนที่ `env.MCP_KEY_PEPPER` ไม่ได้ตั้งค่า (ว่าง/`undefined`) | n/a | n/a | n/a | **= 500** พร้อม log ฝั่งเซิร์ฟเวอร์ที่อ่านรู้เรื่อง (มีข้อความชี้ชัดว่า `MCP_KEY_PEPPER` หาย) — **ห้าม**คำนวณ HMAC จาก `undefined` แล้วรับคีย์ไปเงียบ ๆ (สเปก §10) | n/a | ⚠️ **ปิดได้ครึ่งเดียวในเครื่องนี้** · ครึ่งที่เป็นโค้ดของเรา — `src/lib/mcp/keys.ts` โยน `MCP_KEY_PEPPER is not configured` ตั้งแต่ตอนโหลดโมดูล และ**ไม่คำนวณ HMAC จาก `undefined`** — ปิดแล้วด้วย `verify-mcp` (โหลดไฟล์จริงในลูกโพรเซสที่ไม่มี pepper ผ่าน `module.registerHooks`) · ครึ่งที่เป็น **HTTP 500** ปิดไม่ได้เพราะต้องรัน `next dev` ตัวที่สองที่ไม่มี pepper ซึ่งจะแย่ง `.next` กับเซิร์ฟเวอร์ที่รันอยู่และทำให้ของคนอื่นพัง · 500 เป็นพฤติกรรมมาตรฐานของ Next เมื่อโมดูลของ route โยนตอน import — **ตรวจตอน deploy** ด้วยการลบ `MCP_KEY_PEPPER` ออกจาก preview แล้วยิง `POST /api/mcp/<คีย์>` ครั้งเดียว |

---

## P9-API — endpoint จัดการคีย์ (`POST /api/settings/mcp-keys`)

> 🔴 ก่อนหน้านี้ทุกคีย์ที่ตัวตรวจใช้ถูก mint ด้วย SQL ตรง ๆ ผ่าน `mkKey()` — route นี้เป็น
> auth path จริง (ออกคีย์ที่อ่านข้อมูลทั้งบริษัทได้) แต่ไม่เคยถูกยิงผ่าน HTTP สักครั้งในตัวตรวจ
> ที่รันได้ · P9-UI-04 ที่ดูเหมือนครอบ `key_prefix` อยู่แล้วจริง ๆ เทียบค่าที่ `mkKey()` คำนวณเอง
> ด้วย `keyPrefix()` กับสิ่งที่มันเขียนลงฐานข้อมูลเอง — ฟังก์ชันเดียวกัน อินพุตเดียวกัน ทั้งสองฝั่ง
> จึงพิสูจน์แค่ว่าหน้าเว็บเรนเดอร์คอลัมน์นั้นออกมา ไม่ได้พิสูจน์ว่า route คำนวณ prefix จากคีย์ลับถูกต้อง

| ID | Trigger | Role | UI | UX | API | DB | สถานะ |
|----|---------|------|----|----|-----|----|--------|
| P9-API-01 | `POST /api/settings/mcp-keys` ด้วย `{label}` | owner | n/a | n/a | **= 201** · `body.key` เป็นคีย์ลับรูปแบบ `k_...` | `mcp_keys.key_prefix` ที่บันทึกไว้ **เท่ากับ 10 ตัวแรกของ `body.key` ที่ route คืนมาจริง** (ค่าที่เก็บเทียบกับค่าลับที่คืนออกไป ไม่ใช่เทียบฟังก์ชันกับตัวเอง) | ✅ `verify-mcp` — เพิ่มใหม่ 31 ส.ค. 2569 เพื่อปิดช่องว่างที่ P9-UI-04 เทียบค่ากับตัวเอง · คีย์ที่สร้างระหว่างตรวจถูกลบทิ้งใน `finally` เหมือนคีย์อื่นทั้งหมด |

---

## P9-UI — หน้า `/mcp`

| ID | Trigger | Role | UI | UX | API | DB | สถานะ |
|----|---------|------|----|----|-----|----|--------|
| P9-UI-01 | เปิด `/mcp` | site_supervisor | ถูก **redirect ออก** ไม่ใช่ 200 — เช็ค role ใน `layout.tsx` ก่อน Suspense (`CLAUDE.md` §17 ข้อ 6) | n/a | n/a | n/a | ✅ `verify-mcp` |
| P9-UI-02 | เปิด `/mcp` | anon | ถูก **redirect** — `307` และ `location` ลงท้ายด้วย `/login` | n/a | n/a | n/a | ✅ `verify-mcp` — location จริงคือ `/login?next=%2Fmcp` (พากลับมาที่ `/mcp` หลังล็อกอิน) ตัวตรวจจึงเทียบที่ **path** ไม่ใช่ท้ายสตริง |
| P9-UI-03 | เปิด `/mcp` ตอนยังไม่เคยสร้างคีย์เลย | owner | สถานะ**ว่าง**: ข้อความอธิบายว่ายังไม่มีคีย์ + ปุ่มสร้างคีย์ — ไม่ใช่ตารางที่มีหัวคอลัมน์ลอยไม่มีแถว | n/a | n/a | n/a | ✅ `verify-mcp` |
| P9-UI-04 | เปิด `/mcp` หลังมีคีย์อย่างน้อย 1 ใบ | owner | ตารางคีย์มีครบ: ชื่อ · `key_prefix` (`k_xxxxxxxx…`) · สร้างเมื่อ · ใช้ล่าสุด · ปุ่มเพิกถอน — ครบทุกคอลัมน์ | n/a | n/a | n/a | ✅ `verify-mcp` |
| P9-UI-05 | สี่สถานะของหน้า `/mcp` | owner | `[data-state="skeleton"]` โผล่ก่อนข้อมูลจริงมา (โครงร่าง) · ฉีดความล้มเหลวแล้วเห็นการ์ดผิดพลาด + ปุ่ม `ลองใหม่` ที่ **enabled** · สถานะว่างปิดแล้วที่ `P9-UI-03` · สถานะสำเร็จปิดแล้วที่ `P9-UI-04` | `aria-busy="true"` ระหว่างโหลด | n/a | n/a | ✅ `verify-mcp` — **แก้แล้ว 31 ส.ค. 2569:** `src/app/(app)/mcp/loading.tsx` เปลี่ยนไปใช้ `<PageSkeleton rows={3} titleWidth="w-36" />` เหมือนอีก 11 หน้า จึงมี `data-state="skeleton"` และ `aria-busy="true"` แล้ว · ตัวตรวจอ่านจาก **บอดี้ที่ Next สตรีมออกมาจริง** ของ `GET /mcp` (fallback ของ Suspense ถูกส่งออกก่อนเนื้อหา) ไม่ใช่จากซอร์ส — ก่อนแก้แถวนี้แดง · อีกสามสถานะ: ว่าง = `P9-UI-03` · สำเร็จ = `P9-UI-04` · ผิดพลาด = `page.tsx` วาด `<DataError>` (มี `data-state="error"` + ปุ่ม `ลองใหม่` ที่กดได้จริง) ซึ่งยังปิดด้วยการอ่านโค้ด ไม่ได้ฉีดความล้มเหลวเข้าไปดูของจริง |
| P9-UI-06 | เปิด `/mcp` เมื่อ origin ของ request เป็น `localhost`/`127.`/`0.0.0.0`/`[::1]`/`192.168.`/`10.`/`172.16-31.` | owner | element ที่มี `data-testid="mcp-origin-warning"` **ปรากฏใน DOM จริง** (ไม่ใช่ซ่อนด้วย `display:none`/`hidden`) และข้อความในนั้นมีคำว่า **"localhost"** อยู่จริง | n/a | n/a | n/a | ✅ `verify-mcp` — **แก้แล้ว 31 ส.ค. 2569:** เติม `data-testid="mcp-origin-warning"` ที่กล่องเตือนใน `src/app/(app)/mcp/mcp-client.tsx` · แถวนี้จึงผูกกับ **hook ที่ตั้งใจให้เป็น hook** ไม่ใช่กับข้อความที่เปลี่ยนได้ทุกวัน |
| P9-UI-07 | กดปุ่ม "เพิกถอน" แล้วกดยืนยันใน dialog | owner | แถวของคีย์นั้น**หายจากตารางทันที** โดยไม่ต้องรีเฟรชหน้า | ต้องผ่าน radix confirm dialog ก่อนเพิกถอนจริง — ปิด dialog โดยไม่ยืนยัน แถวยังอยู่ | n/a | `mcp_keys.revoked_at` ของแถวนั้น **ไม่เป็น null** | 👤 ดูกับ Chrome จริง 31 ส.ค. 2569 — กด “เพิกถอน” แล้ว radix dialog เปิดพร้อมหัวเรื่อง “เพิกถอน “<ชื่อคีย์>” ?” และปุ่ม ยกเลิก/เพิกถอน · กด `Escape` ปิดกล่องโดย**แถวยังอยู่** · เปิดใหม่แล้วกดยืนยัน → ปุ่มยืนยัน `disabled` พร้อม `svg.animate-spin` ระหว่างรอ → กล่องปิดเมื่อสำเร็จ → **แถวหายจากตารางโดยไม่ได้รีโหลดหน้า** และสถานะว่างกลับมา · ตรวจฐานข้อมูลหลังจากนั้น `mcp_keys.revoked_at` ของแถวนั้นไม่เป็น null · คีย์ที่สร้างตอนตรวจถูกลบทิ้งแล้ว |
| P9-UI-08 | กดปุ่ม "สร้างคีย์" → กรอกชื่อเครื่อง → ยืนยัน | owner | โชว์ **URL เต็มครั้งเดียว** + ปุ่มคัดลอก + ข้อความเตือนว่า "จะไม่เห็นอีก" | ปิด dialog แล้วเปิดหน้าใหม่ **ไม่เห็น secret เดิมซ้ำอีก** ที่ไหนเลย | n/a | `mcp_keys` **+1 แถว** | 👤 ดูกับ Chrome จริง 31 ส.ค. 2569 — กรอกชื่อเครื่องแล้วกด “ออกคีย์ใหม่” → เห็น URL เต็ม `http://localhost:3100/api/mcp/k_…` ครั้งเดียว พร้อมปุ่ม “คัดลอก” และข้อความ “ปิดหน้านี้แล้วจะไม่เห็นค่านี้อีก” · `mcp_keys` เพิ่มขึ้น 1 แถว · **รีโหลดหน้าแล้วค้นทั้ง DOM ด้วย `/\/api\/mcp\/k_[A-Za-z0-9_-]{43}/` ไม่เจออีกเลย** เหลือแค่ `k_xxxxxxxx…` |
| P9-UI-09 | กดปุ่ม "สร้างคีย์" หรือ "เพิกถอน" ระหว่างรอ API ตอบกลับ | owner | ปุ่มเป็น **disabled พร้อมสปินเนอร์** จนกว่า request จะจบ | กดซ้ำระหว่างรอไม่ทำให้ยิงคำขอซ้ำสอง | n/a | n/a | 👤 ดูกับ Chrome จริง 31 ส.ค. 2569 — snapshot ระหว่างที่คำขอยังไม่จบจับได้ว่าปุ่ม “ออกคีย์ใหม่” อยู่ในสถานะ `disabled` และปุ่มยืนยันเพิกถอนก็ `disabled` พร้อมสปินเนอร์ · กดซ้ำระหว่างรอไม่เกิดคำขอที่สอง (`busy` กันไว้ที่ต้นฟังก์ชันทั้ง `create()` และ `revoke()`) · ตรวจ 390 / 768 / 1440 ทั้งโหมดสว่างและมืด: ไม่มีอิลิเมนต์ไหนล้นขอบขวาเลยสักตัว (วัดขอบขวาของทุกอิลิเมนต์ ไม่ใช่ `scrollWidth` — §17 ข้อ 7) และไม่มี error/warning ใน console |

---

## P9-SHIP — ตรวจรับปลายทางจริง

| ID | Trigger | Role | UI | UX | API | DB | สถานะ |
|----|---------|------|----|----|-----|----|--------|
| P9-SHIP-01 | ต่อ URL จากหน้า `/mcp` เข้า claude.ai จริง (Add custom connector) แล้วถามคำถามที่ต้องใช้ข้อมูลจริง | owner | เจ้าของถามคำถามจาก Claude แล้วได้คำตอบที่ตรงกับตัวเลขบนหน้าจอแอปเป๊ะ ๆ | n/a | n/a | n/a | 👤 — **ปิดในเครื่องไม่ได้**: Claude เรียกจากคลาวด์ของ Anthropic ไม่ใช่จากเครื่องผู้ใช้ `localhost` จึงไม่มีทางถึง และ `LOOP.md` ยังไม่อนุญาตให้ deploy หรือเปิด tunnel ในเฟสนี้ · **ขั้นตอนที่เจ้าของต้องทำตอน deploy:** **1)** ตั้ง `MCP_KEY_PEPPER` ใน Vercel ให้เป็นค่าเดียวกับในเครื่อง **2)** ใช้ URL production ไม่ใช่ preview (Deployment Protection ของ preview ตอบกำแพงล็อกอินที่ connector ผ่านไม่ได้) **3)** ออกคีย์ใหม่จากหน้า `/mcp` บน production **4)** เพิ่ม connector ใน claude.ai ด้วย URL นั้น แล้วสลับ tools จาก `Needs approval` เป็น `Always allow` |

---

## สรุป

เกณฑ์ปิดเฟส: `node scripts/verify-mcp.mjs` (Task 12) ลงท้ายด้วย **`· ตก 0`**
— ไม่ใช่จำนวนแถวที่ตายตัว เพราะตัวเลขรวมจะเปลี่ยนทุกครั้งที่มีคนเพิ่มแถวในไฟล์นี้

- แถวที่ `-DB-` `-FN-` `-PROTO-` `-TOOL-` `-SEC-` `-API-` ปิดด้วย `✅` เมื่อมี `scripts/verify-mcp.mjs` รองรับเท่านั้น
- แถว `-UI-` ปิดด้วย `👤` จากการตรวจในเบราว์เซอร์จริงที่ความกว้าง **390 / 768 / 1440** ทั้งโหมดสว่างและมืด ตาม `CLAUDE.md` §16
- แถว `P9-SHIP-01` เป็น `👤` ตั้งแต่ต้น ปิดในเครื่องไม่ได้ตามเหตุผลในแถว — **ห้ามติ๊ก `✅` จากการเดา**
- **ห้ามเหลือ `☐` แม้แต่แถวเดียว** ตอนปิดเฟส (`CLAUDE.md` §17 ข้อ 10) — ปิดไม่ได้ต้องเปลี่ยนเป็น `👤` หรือ `⚠️` พร้อมเหตุผลที่เขียนไว้จริงในแถว ไม่ใช่ปล่อยว่าง

**กฎการติ๊ก** — แถวที่ ID มี `-DB-` `-FN-` `-PROTO-` `-TOOL-` `-SEC-` `-API-` ต้องมี ID อยู่ใน label ของ
`check()` ที่รันได้จริงใน `scripts/verify-mcp.mjs` ตัวอักษรตรงกับ ID ในไฟล์นี้เป๊ะ ๆ — เพื่อให้
`node scripts/verify-matrix.mjs` (ถ้ามี) กระทบยอดได้

---

## ผลการไล่ปิดจริง — 31 ส.ค. 2569

`node scripts/verify-mcp.mjs` → **`42 แถว: ผ่าน 42 · ตก 0`**

| สถานะ | จำนวน | แถว |
|---|---|---|
| `✅` มีคำสั่งรันได้รองรับ | 42 | ทุกแถว `-DB-` `-FN-` `-PROTO-` `-TOOL-` · `-SEC-` ยกเว้น 06 · `-UI-` 01–06 |
| `👤` ดูกับเบราว์เซอร์จริง | 4 | `P9-UI-07` `P9-UI-08` `P9-UI-09` `P9-SHIP-01` |
| `⚠️` ปิดได้ครึ่งเดียว | 1 | `P9-SEC-06` (ครึ่ง HTTP 500 ต้องตรวจตอน deploy) |
| `❌` ตกจริง | 0 | — |

**รอบแรกได้ `ผ่าน 39 · ตก 2` และมีอีกแถวที่ตกจากการอ่านซอร์ส** — ทั้งสามเป็นข้อบกพร่องจริง
ในโค้ดของงานก่อนหน้า ไม่ใช่ของตัวตรวจ · แก้แล้วทั้งสามเมื่อ 31 ส.ค. 2569 (รายละเอียดอยู่ใน
ช่องสถานะของ `P9-TOOL-08` `P9-UI-05` `P9-UI-06`) · **ไม่มีแถวไหนถูกลบหรือถูกลดความเข้ม
เพื่อให้ตัวเลขสวย** — และ `P9-UI-05` กับ `P9-UI-06` กลายเป็นแถวที่ **รันได้** แทนที่จะเป็นแถวสายตา

พร้อมกันนั้นเฟสนี้ยังทำให้สามแถวของเฟสก่อนแดง และแก้ครบแล้วเช่นกัน:
`P0-UI-01` (เมนู 8 → 9 หลังเพิ่ม “เชื่อมต่อ AI” — แก้ตัวเลขที่ `verify-shell.mjs` คาดหวัง
**ไม่ใช่**เปลี่ยนไปนับจาก `nav.ts` ซึ่งจะเป็นการเทียบโค้ดกับตัวเอง) ·
`P0-UI-09` (ถอด emoji ออกจาก **สตริง** ใน `src/lib/mcp/tools.ts` — ที่อยู่ในคอมเมนต์ไม่แตะ) ·
`P6-DB-01` (`mcp_call_log` เข้ารายการยกเว้น audit trigger หมวดเดียวกับ `login_attempts`
พร้อมเหตุผลเขียนไว้ในไฟล์ · `mcp_keys` **ยังต้องมี trigger** เพราะเป็นตารางของยืนยันตัวตน)

**ข้อมูลตัวอย่างของตัวตรวจ:** `verify-mcp.mjs` สร้างไซต์ 2 · รายการ 6 · คนงาน 1 · การลงชื่อ 1 · งวด 2
ที่ชื่อขึ้นต้นด้วย “ตรวจรับ MCP ชั่วคราว” แล้วลบทิ้งใน `finally` ทุกครั้ง — ทุกยอดต่างกันหมดและ
ไม่มีตัวไหนเป็นศูนย์ เพื่อให้การหยิบคอลัมน์ผิดหรือสลับรายรับกับต้นทุนทำให้ `P9-FN-03` แดงจริง
(บนฐานข้อมูลว่างการเทียบตัวเลขคือ `0 === 0` ซึ่งเขียวตลอดกาล)

**ยังไม่ทำในเฟสนี้ — ตัดออกโดยตั้งใจ (สเปก §2)**
- tool ที่เขียนข้อมูล (อนุมัติ / บันทึกรายจ่าย) — ให้ AI กดอนุมัติแทนเจ้าของคือความเสี่ยงคนละระดับ
- OAuth — คีย์ธรรมดาก็ถูกต้องตามสเปก MCP (Authorization เป็น OPTIONAL) และง่ายกว่ามากสำหรับคนที่ไม่ชำนาญคอม
- `run_sql` แบบเปิด — tool ที่รับ SQL ดิบคือช่องเขียน/อ่านทุกอย่างที่ปลอมตัวเป็น tool อ่านอย่างเดียว
- คีย์แบบจำกัดสิทธิ์รายไซต์ — มีแค่เจ้าของกับหัวหน้าไซต์ และหัวหน้าไซต์ไม่มีเหตุต้องต่อ AI (YAGNI)
- คู่มือสอนส่งคีย์ทาง `Request headers` — รองรับที่ endpoint แต่ไม่เขียนในคู่มือ (สองทางเลือก = สายโทรเข้า)
