# LOOP.md — runbook สร้างระบบจัดการโปรเจ็คงานรับเหมาก่อสร้าง

> **อ่านไฟล์นี้ + `.loop/state.json` เป็นอย่างแรกทุกครั้งที่เริ่มทำงาน** ก่อนตัดสินใจอะไรทั้งสิ้น
>
> ไฟล์นี้ควบคุม **การลงมือตามแผนที่อนุมัติแล้ว** เท่านั้น ไม่ได้ให้อำนาจตัดสินว่าจะสร้างอะไร
> การออกแบบถูกอนุมัติเรียบร้อยแล้วเมื่อ **30 ส.ค. 2569** ที่ [`docs/design/demo.html`](docs/design/demo.html)
> แผนอยู่ที่ [`CLAUDE.md`](CLAUDE.md) · ข้อตกลงดีไซน์อยู่ที่ [`docs/design/DESIGN.md`](docs/design/DESIGN.md)
>
> ถ้าเจองานที่ยัง**ไม่เคยถูกออกแบบ** ให้หยุดแล้วกลับไปที่ `kp-design-brainstorm` — loop ข้ามขั้นนี้ไม่ได้

---

## 0 · Environment — ปักหมุด ห้ามเดา

| อะไร | ค่า |
|---|---|
| Supabase MCP | project_ref **`zvzaepnyxihyzheihwhz`** (ชื่อโปรเจ็ค `Cpie`) · region `ap-southeast-1` |
| ตรวจก่อนแตะ DB ทุกครั้ง | `get_project_url` ต้องตรงกับ `NEXT_PUBLIC_SUPABASE_URL` ใน `.env.local` |
| หมายเหตุ PAT | เป็น token **ผูกเฉพาะโปรเจ็คนี้** — `/v1/organizations` คืนค่าว่าง สร้าง dev branch ไม่ได้ (ปกติ) |
| Cloudflare R2 | bucket `kp-construction-slips` · ตรวจแล้วอ่าน/เขียน/ลบได้จริง 30 ส.ค. 2569 |
| git | **local เท่านั้น ยังไม่มี remote** · branch `main` |
| dev port | `3000` (ถ้าไม่ว่างและไม่ใช่ของเรา → ใช้พอร์ตอื่น **ห้ามฆ่าของคนอื่น**) |
| ห้ามแตะ | `.env*` · `docs/design/demo.html` · `docs/design/DESIGN.md` (สองไฟล์หลังคือสัญญาที่อนุมัติแล้ว จะแก้ต้องถาม) |

### อนุญาตล่วงหน้า — ไม่เขียนไว้ = ไม่อนุญาต

| การกระทำ | อนุญาต |
|---|---|
| `git init` + commit ลง `main` ในเครื่อง | **ใช่** |
| push ขึ้น remote | **ไม่** (ยังไม่มี remote และยังไม่ได้ติดตั้ง `gh`) |
| deploy Vercel | **ไม่** ← เป็นสิทธิ์คนละอันกับ push |
| `apply_migration` ตรงโปรเจ็ค `zvzaepnyxihyzheihwhz` | **ใช่** |
| ติดตั้ง npm package ตามที่ระบุใน `CLAUDE.md` §3 | **ใช่** |
| เปิด Chrome ผ่าน MCP เพื่อทดสอบ | **ใช่** (ตาม §5) |

---

## 1 · Resume — ลำดับที่ทำทุกครั้งที่กลับมา

1. อ่าน `.loop/state.json` — เฟส/task ไหนเสร็จ ค้างที่ไหน
2. **ยืนยันด้วย git ไม่ใช่ความจำ** — `git log --oneline | head -20` และ `git status --short`
   **state ขัดกับ git → เชื่อ git** แล้วแก้ state ให้ตรง
3. ตรวจของค้างจากรอบก่อน: `list_pages` · `netstat -ano | findstr :3000` · อ่าน `.loop/owned.json`
   → อะไรที่**มีอยู่ก่อนและไม่อยู่ใน `owned.json` = ไม่ใช่ของเรา ห้ามแตะ**
4. เริ่มที่งานแรกที่ยังไม่ `done` — **ทำต่อโดยไม่หยุดถาม**

---

## 2 · หนึ่ง iteration

```
1) เลือกงานแรกที่ยังไม่เสร็จและ dependency ครบ → mark doing
2) แก้แบบเล็กที่สุด (≤ 8 ไฟล์ · เกินนี้ = blocked "too large")
3) รัน gate ของงานนั้น (§3)
4) เขียว → mark done · commit "<type>(<scope>): <desc> [<id>]"
   แดง  → แก้ ≤ 3 รอบ · ยังแดง → blocked + เหตุผล แล้วไปทำงานอื่นที่ทำได้
5) เขียน .loop/state.json + docs/LESSONS.md ลงดิสก์   ← ทุกครั้ง ไม่ใช่เฉพาะจบเฟส
6) เช็คเงื่อนไขหยุด (§6) → ไม่เข้าเงื่อนไข ไป iteration ถัดไป
```

**ข้อ 5 คือข้อที่ทำให้ context ถูกย่อตอนไหนก็ไม่เสียหาย** — iteration ถัดไป *อ่านไฟล์* ไม่ใช่ *นึกย้อน*

---

## 3 · Gate — ต้องเขียวก่อน mark done

```bash
npm run gate     # = typecheck (next typegen && tsc) → verify:contrast → build
```

แยกรันทีละอันได้: `npm run typecheck` · `npm run verify:contrast` · `npm run build`

⚠️ **ห้ามใช้ `npx tsc --noEmit` เปล่า ๆ** — `LayoutProps` / `PageProps` เป็น type ที่ Next
generate ตอน build ถ้า `.next/types` ยังไม่มีจะแดงทั้งที่โค้ดถูก · `npm run typecheck` มี typegen นำหน้าให้แล้ว

⚠️ **ห้ามต่อ `| tail` แล้วดู exit code** — exit code จะกลายเป็นของ `tail` ซึ่งเป็น 0 เสมอ
ใช้ `set -o pipefail` หรืออ่าน `${PIPESTATUS[0]}` หรือรันเดี่ยวแล้ว `echo "exit=$?"`

เพิ่มตามสิ่งที่แตะ:

- [ ] `get_advisors(security)` และ `get_advisors(performance)` ไม่มี ERROR ใหม่ — *ถ้าแตะ DB*
- [ ] ทดสอบ RLS ข้าม role จริง: หัวหน้าไซต์ A อ่าน/เขียนข้อมูลไซต์ B ไม่ได้ — *ถ้าแตะตาราง* (leak = critical)
- [ ] `generate_typescript_types` แล้ว `tsc` ยังเขียว — *ถ้าเปลี่ยน schema*
- [ ] Chrome MCP smoke ตาม `kp-testing-cadence` — *ถ้าแตะ UI*
- [ ] acceptance matrix ของเฟส (§4)

**exit code 0 ไม่ใช่หลักฐาน** — อ่าน output จริงว่าไม่มี error ปนมา
**HTTP 200 ก็ไม่ใช่หลักฐาน** — `error.tsx` ของ Next render ด้วยสถานะ 200 ต้องตรวจ *เนื้อหา*
**"ไม่มี error" ไม่ใช่หลักฐานว่าเขียนสำเร็จ** — RLS ที่บล็อกจะคืน 0 แถวโดยไม่มี error ต้องอ่านจำนวนแถวที่กระทบ

---

## 4 · Acceptance matrix — loop ติ๊กเองได้แค่ไหน

เขียน `docs/test-plan/<phase>.md` **ก่อน** เขียนโค้ดของเฟสนั้น ตาม `kp-acceptance-test-matrix`

- ติ๊กได้ **เฉพาะแถวที่มีคำสั่งซึ่งแดงได้จริง**ผูกอยู่ (มีคำสั่งรันได้ + assert ค่าที่วัดได้)
- แถวที่ต้องใช้ตาคน (push เด้งบนมือถือจริง · ติดตั้ง PWA บนเครื่องจริง · กล้องมือถือ) →
  `manual_verify: true` ติ๊กได้แค่ระดับ *code-green* แล้วรวมไว้ในรายการส่งท้าย
- **ห้ามติ๊กสิ่งที่พิสูจน์ไม่ได้** และห้ามติ๊กจากความจำ

---

## 5 · ของที่เปิด = ของที่ต้องเก็บ

```
ก่อนเริ่ม : list_pages → จด id ที่มีอยู่ = ไม่ใช่ของเรา ห้ามปิด
ระหว่าง   : ใช้ page เดิมซ้ำ · เปิดใหม่เมื่อจำเป็น → จดลง .loop/owned.json ทันที
จบรอบ     : ปิดเฉพาะ page ของเรา · หยุด dev server เฉพาะ PID ที่เราสตาร์ต
```

**จดลงไฟล์ ไม่ใช่จำเอา** · เพดาน: page เกิน 5 หรือมี dev server ตัวที่สอง → **หยุดแล้วรายงาน**

> **ของที่เปิดค้างอยู่ตอนเริ่มรอบแรก:** Chrome page id `2` = เดโม่ดีไซน์ (เปิดโดยเรา ปิดได้)

---

## 6 · ขอบเขตและเงื่อนไขหยุด

| ขอบเขต | ค่า |
|---|---|
| แก้ต่องาน | **3** รอบ → blocked |
| ไฟล์ต่อ iteration | **8** → blocked "too large" |
| iteration ต่อ run | **40** |
| blocked ติดกัน | **3** → หยุดทั้ง run |
| สืบหาสาเหตุ | **วัดครบ 1 รอบแล้วยังทำซ้ำไม่ได้ → หยุด รายงานตัวเลขที่วัดมาจริง + คำถามที่ต้องการคำตอบ** ห้ามขยายขอบเขตการค้นเอง |
| จบเฟส | **ไปต่อทันที ไม่หยุด** (ดู §7) |

**หยุดถามเฉพาะ 4 กรณี**
1. งานที่ย้อนกลับไม่ได้ / ทำลายข้อมูล
2. ความปลอดภัย — คีย์ ความลับ สิทธิ์
3. ผลกระทบนอกเครื่อง — push / deploy / publish (§0 บอกว่ายัง **ไม่อนุญาต** ทั้งคู่)
4. แผนพังจนทุกทางเลือกเป็นการเดา

นอกนั้น **ตัดสินเองแล้วบันทึกลง `.loop/state.json`**:
`Ruling: <ตัดสินอะไร> — <เพราะอะไร> — <ถ้าผิดเสียอะไร>`

เรื่องที่เป็นการตัดสินใจของเจ้าของกิจการ (ชื่อบริษัท · ผู้ลงนาม · หมวดค่าใช้จ่าย · เรตค่าแรง)
→ ใช้ค่าชั่วคราวใน `src/lib/constants.ts` ระบุว่าชั่วคราว **ทำส่วนอื่นต่อ** แล้วรวบรวมแจ้งตอนจบ

---

## 7 · สามสวิตช์ — โปรเจ็คนี้เลือกอะไร

| สวิตช์ | ค่าที่เลือก |
|---|---|
| จังหวะ | **วิ่งยาว P0 → P8 ไม่หยุดท้ายเฟส** |
| branch | **commit ลง `main` ตรง** |
| migration | **apply ตรงโปรเจ็คจริง** |

**เพราะ** โปรเจ็คว่างเปล่า ยังไม่มีข้อมูลลูกค้าจริงสักแถว ไม่มีใครใช้งานอยู่
ความผิดพลาดที่หนักที่สุดคือ *ล้างฐานข้อมูลแล้วเริ่มใหม่* ซึ่งกู้คืนได้ทั้งหมด
และเจ้าของต้องการระยะทาง ไม่ใช่จุดตรวจ · **ต้องเปลี่ยนเป็นคอลัมน์ซ้ายทันทีที่มีข้อมูลลูกค้าจริงเข้าระบบ**

---

## 8 · ห้ามเด็ดขาด

- destructive DB: `DROP` / `TRUNCATE` / `DELETE` ที่ไม่มี `WHERE` บนตารางที่มีข้อมูลจริง
- `git push --force` · `reset --hard` บน commit ที่ push แล้ว · `rm -rf`
- แตะ `.env*` หรือไฟล์ในรายการห้ามแตะ (§0)
- **`cat` ไฟล์ env/settings ทั้งไฟล์** — ดูได้แค่ชื่อคีย์: `awk -F= '/^[A-Z0-9_]+=/{print $1}' .env.local`
  (อ่านทั้งไฟล์ = ความลับหลุดลง transcript ถึงจะ gitignore ถูกแล้วก็ตาม และคนต้องมาหมุนคีย์ใหม่)
- รัน seed ที่ล้างข้อมูลขณะมีคนกำลังทดสอบ
- สร้าง VAPID / `PIN_PEPPER` / `CRON_SECRET` ใหม่ทับของเดิม
- **ไม่มั่นใจว่าปลอดภัย = ไม่ปลอดภัย → หยุด**

---

## 9 · บทเรียน — จดตอนเจอ ไม่ใช่ตอนจบ

เขียนลง [`docs/LESSONS.md`](docs/LESSONS.md) **ทันทีที่จับได้** (context ถูกย่อแล้วรายละเอียดหายหมด)

```markdown
## <อาการที่คนเห็น>
- สาเหตุจริง:  <กลไกจริง ไม่ใช่ที่เดาครั้งแรก>
- หลักฐาน:     <ตัวเลข/สตริงจริง ห้าม "ดูแล้วพัง">
- กฎกันซ้ำ:    <อะไรที่ทำให้ไม่เกิดอีก>
- เจ้าของกฎ:   <ชื่อสกิลที่ควรถือกฎนี้>
```

นับเป็นบทเรียนเมื่อ **โปรเจ็คอื่นก็จะโดนกัดเหมือนกัน** · พิมพ์ผิดไม่นับ

**ตอนจบ build** ยื่นข้อเสนอที่ทำได้เลย ไม่ใช่ถามลอย ๆ เช่น
> รอบนี้ได้ 6 บทเรียน → `thai-saas-ui-kit` ×2 · `supabase-rls-schema` ×3 · `kp-seed-and-reset` ×1 — เพิ่มไหม

---

## 10 · คำถามก่อนเริ่ม — ตอบแล้ว 30 ส.ค. 2569

1. **ขอบเขต** — P0 ถึง P8 ครบทุกเฟส
   *จบเมื่อ:* `tsc` + `build` เขียว · ทุกแถว auto-tick ของ matrix ทั้ง 9 เฟสปิด ·
   seed/reset ใช้ได้จริง · มีรายการ `manual_verify` ส่งให้คนตรวจ · มีข้อเสนออัปเดตสกิล
2. **ใบอนุญาต** — push **ไม่** · deploy **ไม่** · apply migration ลงโปรเจ็คจริง **ใช่**
3. **สามสวิตช์** — วิ่งยาว · commit ลง `main` · apply migration ตรง (ดู §7)
4. **งบ** — 40 iteration ต่อรอบ · แก้ 3 รอบต่องาน · 8 ไฟล์ต่อ iteration · blocked ติดกัน 3 = หยุด
5. **ห้ามแตะ** — `.env*` · `docs/design/demo.html` · `docs/design/DESIGN.md`
6. **ข้อมูลจริง** — **ไม่มี** · โปรเจ็ค Supabase สร้าง 29 ส.ค. 2569 ยังว่าง · R2 มี 0 ไฟล์ · ไม่มีใครทดสอบอยู่
7. **ค้างจากรอบก่อน** — ไม่มี นี่คือรอบแรก · มีแต่ Chrome page id `2` ที่เราเปิดดูเดโม่ (เป็นของเรา)
8. **ตัดสินแทนได้แค่ไหน** — ทุกเรื่องที่ไม่เข้า 4 ข้อใน §6
   *ที่ต้องรอเจ้าของจริง ๆ เพิ่ม:* ชื่อบริษัทจริง · อีเมลเจ้าของที่จะใช้ล็อกอิน · รายการหมวดค่าใช้จ่ายจริง
   · เรตค่าแรงจริง → ใช้ค่าชั่วคราวใน `src/lib/constants.ts` ไปก่อน แล้วรวบรวมแจ้งตอนจบ

---

## 11 · งานที่ต้องทำก่อน iteration แรก

1. `git init` · `git add .` · commit แรก (`.env.local` ต้องไม่ติดไปด้วย — ตรวจ `git status` ก่อน)
2. สร้าง `.loop/state.json` จากเฟส P0–P8 ใน `CLAUDE.md` §14
3. สร้าง `.loop/owned.json` และ `docs/LESSONS.md`
4. `get_project_url` เทียบกับ `.env.local` — **ต้องตรงก่อนแตะ DB ครั้งแรก**
