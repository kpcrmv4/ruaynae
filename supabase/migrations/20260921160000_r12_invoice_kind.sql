-- R12 (ต่อ) · เพิ่ม **ใบแจ้งหนี้** เป็นชนิดเอกสารที่สาม (คำสั่งเจ้าของ 21 ก.ย. 2569)
--
-- ไฟล์ `cm011-แจ้งหนี้-สถาปัต-มช.xlsx` ที่เจ้าของส่งมาตั้งแต่แรกคือใบแจ้งหนี้
-- รอบที่แล้วใช้เป็น fixture ของสูตรอย่างเดียว (D6) · รอบนี้เปิดใช้จริง
--
-- ลำดับงานของจริง: **ใบเสนอราคา → ใบแจ้งหนี้ → ใบเสร็จรับเงิน**
-- จึงแทรกค่าใหม่ไว้ **ก่อน `receipt`** เพื่อให้ลำดับของ enum อ่านเหมือนลำดับงาน
-- (`order by kind` ที่ไหนก็ตามจะเรียงตามเส้นทางเอกสาร ไม่ใช่ตามวันที่เพิ่มค่า)
--
-- 🔴 ไฟล์นี้มีคำสั่งเดียวโดยตั้งใจ — Postgres ห้ามใช้ค่า enum ที่เพิ่งเพิ่ม
-- ภายในทรานแซกชันเดียวกับที่เพิ่มมัน · อะไรที่ต้องอ้างถึง `'invoice'` ตรง ๆ
-- ต้องอยู่ใน migration ถัดไป ไม่ใช่ไฟล์นี้
--
-- สิ่งที่ **ไม่ต้อง** แก้ เพราะออกแบบเผื่อไว้ตั้งแต่ต้น:
--   · `doc_counters` มี `kind` เป็น pk → ใบแจ้งหนี้เดินเลขชุดของตัวเองทันที
--   · `documents_kind_no_uniq` เป็น unique (kind, doc_no) → เลขไม่ก้าวก่ายชนิดอื่น
--   · `next_doc_no(p_kind public.doc_kind)` และ `issue_document()` รับทุกค่าของ enum
--   · `doc_recalc()` ไม่สนใจชนิดเลย มันคิดจากบรรทัดอย่างเดียว

alter type public.doc_kind add value if not exists 'invoice' before 'receipt';
