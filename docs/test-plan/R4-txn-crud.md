# R4 · แก้ไข/ลบรายการที่บันทึกแล้ว + รายการในหน้าโครงการ — ตารางตรวจรับ

> คำสั่งเจ้าของ 31 ส.ค. 2569: *"ทำให้ค่าใช้จ่ายที่บันทึกแล้วสามารถ CRUD ได้
> และในหน้ารายละเอียดโครงการ ให้แสดงรายการรายรับรายจ่ายที่ด้านล่างด้วย"*
>
> **มีการแก้กฎที่ฐานข้อมูล** (guard trigger + policy) → ดู `supabase/migrations/20260901160000_r4_txn_crud.sql`
> และแถว `P2-DB-05` / `P2-DB-11` ใน [`P2.md`](P2.md) ที่เปลี่ยนความหมายไปพร้อมกัน

**สถานะ:** `☐` ยังไม่ตรวจ · `✅` ผ่านโดยมีคำสั่งรันได้รองรับ · `❌` ตก · `⚠️` blocked · `👤` ต้องคนตรวจ

> 👤 ของรอบนี้ = แถวที่ต้องมี **dev server + เบราว์เซอร์จริง** — เครื่องที่ทำรอบนี้
> ไม่มี `.env.local` ของโปรเจ็คจริง จึงรัน `scripts/verify-txn-edit.mjs` ไม่ได้
> · แถว DB ทั้งหมดถูกตรวจจริงบนสคีมาจริงด้วยธุรกรรมที่ **ย้อนกลับทั้งก้อน**
> (ผลของแต่ละแถวบันทึกไว้ในช่อง "หลักฐาน") · ไล่ปิดแถว 👤 ตอนรัน
> `node scripts/verify-txn-edit.mjs` บนเครื่องพัฒนา ทั้งสองธีม · 390/768/1440 (CLAUDE.md §16)

---

## 0 · สำรวจพื้นผิวที่แตะ

| นับอะไร | จากไหน | จำนวน |
|---|---|---|
| Migration | `20260901160000_r4_txn_crud.sql` (guard + 4 policy + notify) | **1** |
| API ใหม่ | `POST /api/transactions/[id]/attachments` · `DELETE /api/attachments/[id]` | **2** |
| API ที่แก้ | `PATCH`/`DELETE /api/transactions/[id]` (เช็คสิทธิ์ให้ได้เหตุผล) · `POST /api/transactions` (ใช้ helper ร่วม) | **2** |
| หน้าที่แก้ | `/ledger` · `/sites/[id]` | **2** |
| คอมโพเนนต์ใหม่ | `components/ledger/txn-edit.tsx` · `components/ledger/txn-row.tsx` | **2** |
| lib ใหม่ (แยกของเดิมออกมาใช้ร่วม) | `lib/attachments.ts` · `lib/slip-upload.ts` · `canModifyTxn()` | **3** |

---

## 1 · R4-DB — กฎที่ฐานข้อมูลบังคับ

| ID | Trigger | Role | ผลที่คาด | หลักฐาน | สถานะ |
|----|---------|------|----------|---------|--------|
| R4-DB-01 | แก้ `amount` ของรายการที่ `approved` | owner | สำเร็จ · ค่าใหม่ถูกเขียนจริง | `rows=1 amount=1234` (จาก 1000) | ✅ |
| R4-DB-02 | ลบรายการที่ `approved` | owner | สำเร็จ · `audit_log` มี `action='DELETE'` พร้อม `before` ทั้งแถว | `rows=1` · `audit DELETE rows=1` | ✅ |
| R4-DB-03 | แก้รายการของตัวเองที่ถูก **ตีกลับ** | site_supervisor | สำเร็จ · สถานะกลับเป็น **`pending`** เอง · `rejected_reason` ถูกล้าง | `rows=1 status=pending reason=(ว่าง)` | ✅ |
| R4-DB-04 | แก้ `amount` ของรายการที่ `approved` | site_supervisor | โดน policy ตัด **0 แถว** · ยอดเดิมคงอยู่ | `rows=0` | ✅ |
| R4-DB-05 | ลบรายการที่ `approved` | site_supervisor | โดน policy ตัด **0 แถว** | `rows=0` | ✅ |
| R4-DB-06 | หลัง R4-DB-03 | — | เจ้าของได้แจ้งเตือน `txn_pending` หัวข้อ "รายการที่ตีกลับถูกแก้แล้วส่งใหม่" | `notifications rows=1` | ✅ |
| R4-DB-07 | ลบรายการของตัวเองที่ถูกตีกลับ | site_supervisor | สำเร็จ — ของที่ตีกลับแล้วไม่แก้ ต้องทิ้งได้ ไม่ใช่ค้างในระบบตลอดกาล | `verify-txn-edit` R4-DB-07 | 👤 |
| R4-DB-08 | `anon` ยิง `PATCH`/`DELETE` ที่ `transactions` และ `attachments` | anon | **0 แถว** ทุกกรณี · จำนวนแถวเท่าเดิม | `verify-txn-edit` R4-DB-08 | 👤 |

## 2 · R4-API — endpoint

| ID | Trigger | Role | ผลที่คาด | สถานะ |
|----|---------|------|----------|--------|
| R4-API-01 | `PATCH /api/transactions/[id]` แก้เนื้อรายการที่ `approved` | owner | `200` · ค่าที่ส่งไปถูกเขียนจริงทุกฟิลด์ | 👤 |
| R4-API-02 | `DELETE /api/transactions/[id]` รายการที่ `approved` | owner | `200` · แถวหายจากฐานข้อมูล | 👤 |
| R4-API-03 | `PATCH` รายการที่ `approved` | site_supervisor | `403` `EDIT_FORBIDDEN` (ไม่ใช่ `200` ที่โดน 0 แถวเงียบ ๆ) | 👤 |
| R4-API-04 | `DELETE` รายการที่ `approved` | site_supervisor | `403` `DELETE_FORBIDDEN` · แถวยังอยู่ | 👤 |
| R4-API-05 | `POST /api/transactions/[id]/attachments` เกิน `MAX_ATTACHMENTS` | owner | `400` `TOO_MANY_ATTACHMENTS` · ไม่มีแถวสลิปเพิ่ม | 👤 |
| R4-API-06 | `POST …/attachments` ด้วยคีย์ที่ไม่มี `upload_intents` ของตัวเอง | owner | `400` `INTENT_NOT_FOUND` — คีย์ที่เดาเอาเองแปะไม่ติด | 👤 |
| R4-API-07 | `DELETE /api/attachments/[id]` ของรายการที่ไม่มีสิทธิ์แก้ | site_supervisor | `403`/`404` · แถวสลิปยังอยู่ | 👤 |
| R4-API-08 | ยิง endpoint ใหม่ทั้งสองตอน **ไม่ล็อกอิน** | anon | `401` JSON ทั้งคู่ (ไม่ใช่ `307` ไปหน้า HTML) | 👤 |

## 3 · R4-UI — หน้าจอ

| ID | Trigger | Role | สิ่งที่ต้องเห็น | สถานะ |
|----|---------|------|----------------|--------|
| R4-UI-01 | เปิด `/ledger` | owner | ทุกแถวมีปุ่มดินสอ (`aria-label` ขึ้นต้น "แก้ไขรายการ") | 👤 |
| R4-UI-02 | เปิด `/ledger` | site_supervisor | แถวที่ `approved` **ไม่มี**ปุ่มดินสอ · แถว `pending`/`rejected` ของตัวเองมี | 👤 |
| R4-UI-03 | กดดินสอ → กล่องแก้ไข | owner | ยอด/หมวด/โครงการ/วันที่/วิธีจ่าย/รายละเอียด/สลิป ครบ · ชนิดรายการแก้ไม่ได้ | 👤 |
| R4-UI-04 | กล่องแก้ไขของรายการที่ `approved` | owner | มีแถบเตือนสีแดงว่ายอดรวมย้อนหลังจะเปลี่ยน และมีบันทึกใน `/audit` | 👤 |
| R4-UI-05 | กด "ลบรายการ" | owner | ต้องยืนยันอีกชั้นก่อน (ไม่ลบทันที) · ปุ่มยืนยันคนละฝั่งกับ "ย้อนกลับ" | 👤 |
| R4-UI-06 | หมวดหรือโครงการของแถวถูกปิดใช้งานไปแล้ว | owner | ค่าเดิมยังถูกเลือกอยู่ในกล่อง (ไม่เงียบ ๆ เด้งไปค่าอื่น) | 👤 |
| R4-UI-07 | เปิด `/sites/<id>` | owner | ท้ายหน้ามีแผง "รายรับ-รายจ่ายล่าสุด" พร้อมลิงก์ "ดูทั้งหมด" → `/ledger?site=<id>` | 👤 |
| R4-UI-08 | โครงการที่ยังไม่มีรายการ | owner | แผงเดียวกันขึ้นสถานะว่าง ไม่ใช่แผงเปล่า | 👤 |
| R4-UI-09 | เปิด `/sites/<id>` | site_supervisor | เห็นเฉพาะ**รายจ่าย**ของโครงการนั้น (RLS) · ไม่มีชิปชื่อโครงการซ้ำทุกแถว | 👤 |
| R4-UI-10 | เกิน 10 แถว | owner | มีปุ่ม "ดูรายการทั้งหมดของโครงการนี้" ท้ายแผง | 👤 |
| R4-UI-11 | แก้/ลบสำเร็จ | owner | toast ภาษาไทย · ตัวเลขบนหน้า (การ์ดเงิน/แถบ) อัปเดตตามทันที | 👤 |
| R4-UI-12 | จอ 390 / 768 / 1440 · สว่างและมืด | ทั้งคู่ | กล่องแก้ไขไม่ล้นจอ · ปุ่มแตะได้ ≥44px · ไม่มีอะไรถูกตัดหาย | 👤 |

## 4 · แถวที่เปลี่ยนความหมายในเฟสก่อน

| ID | เดิม | ใหม่ | ที่อยู่ |
|----|------|------|--------|
| P2-DB-05 | เจ้าของแก้ยอดหลังอนุมัติ **ไม่ได้** | **หัวหน้าโครงการ**แก้ไม่ได้ · เจ้าของแก้ได้ (P2-DB-05b) | [`P2.md`](P2.md) |
| P2-DB-11 | ลบรายการที่อนุมัติแล้ว **ไม่ได้** | **หัวหน้าโครงการ**ลบไม่ได้ · เจ้าของลบได้ (P2-DB-11b) | [`P2.md`](P2.md) |
