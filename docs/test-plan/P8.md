# P8 · ข้อมูลตัวอย่าง · เช็คลิสต์ก่อนส่งมอบ · ตรวจรับรวม

> เฟสปิดงาน — ไม่มีฟีเจอร์ใหม่ · มีสามอย่าง: ข้อมูลตัวอย่างที่เชื่อถือได้
> · เช็คลิสต์ก่อน deploy ที่ **รันได้** ไม่ใช่จำเอา · และการไล่ตารางทุกเฟสให้ปิด

**สถานะ:** `☐` ยังไม่ตรวจ · `✅` ผ่านโดยมีคำสั่งรันได้รองรับ · `❌` ตก · `👤` ต้องคนตรวจ
· `⚠️` **กลไกตรวจทำงานแล้วและมันกำลังเตือนอยู่** — สคริปต์พิสูจน์ตัวเองได้ว่าจับเจอ
แต่ตัวข้อยังค้าง รอคนไปกดที่ Supabase/Vercel หรือรอไฟล์จากเจ้าของ · **ห้ามติ๊ก `✅`**
เพราะ `✅` แปลว่าเรื่องนั้นเรียบร้อยแล้ว ไม่ใช่แค่ตรวจเจอ

---

## P8-SEED — ข้อมูลตัวอย่าง

> 🔴 **ยอดที่ seed ไว้ต้องเท่ากับยอดที่แอปคำนวณเอง** (CLAUDE.md §13 ข้อ 6)
> เดโม่ที่โชว์สองตัวเลขขัดกันทำให้คนดูเลิกเชื่อทั้งหน้า · สคริปต์ seed
> จึง **ไม่เขียนยอดรวมลงฐานข้อมูลเลย** เขียนแต่รายการดิบ แล้วให้สูตรเดียวกับ
> ที่หน้าจอใช้เป็นคนบวก แล้วเทียบกับตัวเลขที่ตั้งใจไว้

| ID | Trigger | Role | UI | UX | API | DB | สถานะ |
|----|---------|------|----|----|-----|----|--------|
| P8-SEED-01 | รัน `seed-demo.mjs` | n/a | n/a | n/a | n/a | จบด้วยรหัส 0 · ทุกยอดตรงกับที่ตั้งใจ (สคริปต์ตรวจตัวเอง) | ✅ |
| P8-SEED-02 | รันซ้ำครั้งที่สอง | n/a | n/a | n/a | n/a | จำนวนแถวทุกตาราง **เท่าเดิม** — idempotent จริง ไม่ใช่แค่ไม่ error | ✅ |
| P8-SEED-03 | สถานะไซต์ที่ครอบ | owner | มีครบทั้ง 5 แบบ: ใกล้ครบกำหนด · ต้นทุนแซง · ยังไม่ตั้งค่างาน · ปิดงานแล้ว · ยังไม่เริ่ม | n/a | n/a | n/a | ✅ |
| P8-SEED-04 | สถานะรายการเงินที่ครอบ | owner | มีครบ `pending` · `approved` · `rejected` (พร้อมเหตุผล) · ผูกไซต์ · ส่วนกลาง | n/a | n/a | n/a | ✅ |
| P8-SEED-05 | 🔴 ยอดบนหน้าจอ = ยอดที่ seed ตั้งใจ | owner | หน้าภาพรวมแสดง ฿2,850,000 / ฿2,280,000 / 80% และป้ายต้นทุนแซงของไซต์ที่ควรขึ้น | n/a | n/a | ตรงกับ SQL | ✅ |
| P8-SEED-06 | คนที่เบิกเกือบเต็มเพดาน | owner | มีคนที่เหลือเบิกได้ ฿20 — ให้เห็นสถานะ "ใกล้เต็มเพดาน" บนหน้าจอจริง | n/a | n/a | n/a | ✅ |
| P8-SEED-07 | 🔴 `reset.sql` ไม่อยู่ใน `migrations/` | n/a | n/a | n/a | n/a | ไฟล์ลบข้อมูลที่อยู่ใน `migrations/` จะถูกรันอัตโนมัติตอน deploy บนข้อมูลจริง | ✅ |
| P8-SEED-08 | หลังรีเซ็ต | owner | ผู้ใช้ที่ยังล็อกอินค้างอยู่**ไม่ติดวน**ระหว่าง `/` กับ `/login` · เห็นสถานะว่างแทน | n/a | n/a | n/a | ✅ |

---

## P8-SHIP — เช็คลิสต์ก่อนส่งมอบ (รันได้ ไม่ใช่จำเอา)

> CLAUDE.md §18 สั่งไว้ว่า "ให้ P8 เขียนสคริปต์ตรวจข้อนี้ ไม่ใช่จำเอา"
> · เช็คลิสต์ที่เป็นข้อความจะถูกข้ามในวันที่รีบ ซึ่งคือวันที่ deploy เสมอ

| ID | Trigger | Role | UI | UX | API | DB | สถานะ |
|----|---------|------|----|----|-----|----|--------|
| P8-SHIP-01 | 🔴 `SEED_*` และ `ENABLE_DEMO_LOGIN` ใน production | n/a | n/a | n/a | n/a | สคริปต์เตือนถ้ายังตั้งอยู่ — ขั้ว opt-in แปลว่าไม่มีตัวแปร = ปิด | ✅ |
| P8-SHIP-01b | 🔴 ขั้วของปุ่มเดโม่ | n/a | n/a | n/a | ไม่ตั้ง `ENABLE_DEMO_LOGIN` → route ตอบ **404** ไม่ใช่ 403 (403 ยืนยันว่า route มีอยู่) | ไม่มีฝาแฝด `NEXT_PUBLIC_` ที่ drift จาก route ได้ | ✅ |
| P8-SHIP-02 | Leaked Password Protection | n/a | n/a | n/a | อ่านจาก advisors — ต้องเปิดก่อนส่งมอบ (ตอนพัฒนาปิดไว้เพราะปฏิเสธรหัสทดสอบ) | n/a | ⚠️ |
| P8-SHIP-03 | รหัสผ่าน/PIN ของบัญชีทดสอบ | n/a | n/a | n/a | n/a | เตือนถ้า `SEED_OWNER_PASSWORD` หรือ PIN ยังเป็นค่าที่เดาง่าย | ⚠️ |
| P8-SHIP-04 | 🔴 public signup | n/a | n/a | n/a | `GET /auth/v1/settings` ต้องได้ `disable_signup: true` และไม่มี `anonymous` | n/a | ✅ |
| P8-SHIP-05 | pg_cron ของ `sweep-orphans` และ `push-dispatch` | n/a | n/a | n/a | n/a | เตือนว่ายังไม่ได้ตั้ง (ตั้งได้ต่อเมื่อมี URL สาธารณะ) | ⚠️ |
| P8-SHIP-06 | ความลับใน git | n/a | n/a | n/a | n/a | ไม่มีไฟล์ `.env` จริงถูก track · ไม่มีคีย์โผล่ในซอร์ส | ✅ |
| P8-SHIP-07 | advisors ระดับ ERROR | n/a | n/a | n/a | ERROR = 0 ทั้ง security และ performance | n/a | ✅ |
| P8-SHIP-08 | RLS ครบทุกตาราง | n/a | n/a | n/a | n/a | ไม่มีตารางไหนใน `public` ที่ปิด RLS | ✅ |
| P8-SHIP-09 | ไอคอนและ VAPID | n/a | n/a | n/a | n/a | `VAPID_SUBJECT` ต้องเป็น `mailto:` ของอีเมลจริง ไม่ใช่ค่าเปล่า | ✅ |
| P8-SHIP-09b | ไอคอนแอป | n/a | n/a | n/a | n/a | สร้างไอคอนสำรองชุดใหม่ลง temp แล้วเทียบไบต์ — ตรงกัน = ยังเป็นรูปที่ระบบวาดเอง · ต่างกัน = ไฟล์จริงของเจ้าของ | ✅ |

---

## P8-E2E — แถวที่ต้องมีเบราว์เซอร์จริง

> รวมแถวที่ค้าง `☐`/`👤` จากทุกเฟส — toast · ปุ่ม disabled ระหว่างส่ง ·
> โครงร่างตอนโหลด · หน้าจอที่ 390/768/1440 · โหมดมืด · การกดจริง

| ID | Trigger | Role | UI | UX | API | DB | สถานะ |
|----|---------|------|----|----|-----|----|--------|
| P8-E2E-01 | หน่วง RSC แล้วเปลี่ยนหน้า | owner | `[data-state="skeleton"]` โผล่ก่อน แล้ว**หายไป**เมื่อของจริงมา · หัวเรื่องขึ้นครบ | `aria-busy="true"` ระหว่างโหลด | n/a | n/a | ✅ |
| P8-E2E-01b | ขอ HTML ของ 7 หน้าข้อมูล | owner | ทุกหน้ามี `data-state="skeleton"` อยู่ในสตรีม — แปลว่ามี `loading.tsx` ของตัวเองจริง | n/a | n/a | n/a | ✅ |
| P8-E2E-02 | `/ledger?site=<ไม่ใช่ uuid>` → Postgres 22P02 | owner | เห็นการ์ด `โหลดรายการไม่สำเร็จ` + ปุ่ม `ลองใหม่` ที่ **enabled** | กดปุ่มแล้วหน้าไม่ค้างไม่ระเบิด · พอพารามิเตอร์ถูก การ์ดหายไป | n/a | n/a | ✅ |
| P8-E2E-03 | บันทึกรายจ่ายจากหน้า `/entry` | owner | ปุ่มเปลี่ยนเป็น `กำลังบันทึก…` และ **disabled** · มีสปินเนอร์ | `[data-sonner-toast]` ขึ้นหลังสำเร็จ | `POST /api/transactions` | แถวใหม่ถูกลบทิ้งตอนจบ (ตัวตรวจเก็บกวาดของตัวเอง) | ✅ |
| P8-E2E-04 | 7 หน้าข้อมูล × ความกว้าง 390 / 768 / 1440 | owner | **ไม่มีอิลิเมนต์ไหนขอบขวาเกินความกว้างจอ** (ยกเว้นของที่อยู่ในกล่องที่ตั้งใจให้เลื่อน) · <1024px เห็นแถบล่างไม่เห็น sidebar · ≥1024px กลับกัน | n/a | n/a | n/a | ✅ |
| P8-E2E-04b | ยัด div กว้าง 3000px เข้าหน้า | owner | สูตรวัดต้อง**แดง** — พิสูจน์ว่าแถว 04 ไม่ได้เขียวเพราะวัดไม่เป็น | n/a | n/a | n/a | ✅ |
| P8-E2E-05 | กดปุ่มสลับธีม | owner | `html.dark` พลิกค่า · `background-color` และ `color` ของ body **เปลี่ยนจริงทั้งคู่** · ทั้งสองโหมดพื้นหลังไม่โปร่งใส | n/a | n/a | n/a | ✅ |
| P8-E2E-07 | เปิด `/sites/<uuid ที่ไม่มีอยู่>` | owner | n/a | n/a | ต้องได้ **404** ไม่ใช่ 200 — `loading.tsx` ที่อยู่เหนือ segment ทำให้ `notFound()` ตั้งรหัสไม่ได้ | n/a | ✅ |
| P8-E2E-06 | เปิดแจ้งเตือนบนมือถือจริง | owner | ขอสิทธิ์ → ได้รับ push จริง → กดแล้วเปิดหน้าที่ถูก | n/a | n/a | n/a | 👤 |

---

## สรุป

```
รวม 28 แถว
  ✅ ผ่านโดยมีคำสั่งรันได้รองรับ : 24
  ⚠️  กลไกพร้อม แต่ข้อยังค้าง     : 3   (SHIP-02 · SHIP-03 · SHIP-05)
  👤 ต้องมีเครื่องจริง            : 1   (E2E-06 — push บนมือถือ)
  ❌ ตก                          : 0
```

**แถว `P8-E2E-*` รันด้วย** `npm run verify:e2e` (Playwright + Chromium ยิงใส่
dev server จริงและฐานข้อมูลจริง ไม่ mock Supabase) · ตัวห่อจะใส่ข้อมูลตัวอย่าง
ให้ก่อนถ้าฐานว่าง แล้ว**คืนสภาพเดิมตอนจบ** — หน้าที่ว่างเปล่าทำให้แถวพวกนี้
เขียวโดยไม่ได้ตรวจอะไรเลย

**E2E-06 ยังเป็น `👤`** เพราะ push จริงต้องมีเครื่องจริง: Chromium ที่รันแบบ
headless ไม่มี push service ให้ subscribe และการปลอม endpoint ก็ไม่ได้ทดสอบ
สิ่งที่แถวนี้ถามอยู่ดี · ที่เหลือทั้งหมด (VAPID · service worker · subscribe ·
badge · ตัวส่ง) ถูกตรวจแล้วใน `verify-pwa` 27 แถว

**3 ข้อที่ยังค้าง ไม่ใช่บั๊ก แต่เป็นงานที่ต้องทำก่อนส่งมอบ** — `verify-ship.mjs`
พิมพ์ออกมาให้ทุกครั้งที่รัน จึงลืมไม่ได้:

| ข้อ | ต้องทำอะไร | ทำได้เมื่อไหร่ |
|---|---|---|
| SHIP-02 | เปิด Leaked Password Protection ที่ Supabase | หลังเปลี่ยนรหัสทดสอบแล้ว (ไม่งั้นล็อกอินเดโม่ไม่ได้) |
| SHIP-03 | เปลี่ยนรหัสผ่านเจ้าของและ PIN ทั้งสองใบ | ก่อนส่งมอบ |
| SHIP-05 | ตั้ง pg_cron: `sweep-orphans` รายชั่วโมง · `push-dispatch` รายนาที | หลัง deploy (ต้องมี URL สาธารณะก่อน) |

**กฎการติ๊ก** — แถวที่ ID มี `-SEED-` `-SHIP-` ต้องมี ID อยู่ใน label ของ check
ที่รันได้จริงใน `scripts/**` · กระทบยอดด้วย `node scripts/verify-matrix.mjs`

**ทำไมแถว `👤` ถึงไม่ถูกติ๊กโดยอัตโนมัติ** — สกิล `kp-autonomous-loop` ห้าม
ลูปติ๊กแถวที่ตัวเองพิสูจน์ไม่ได้ · แถวพวกนี้ต้องมีคนเปิดเบราว์เซอร์จริง
หรือมี Playwright ที่รันได้ · การติ๊กจากความจำทำให้ตารางทั้งใบเชื่อไม่ได้
