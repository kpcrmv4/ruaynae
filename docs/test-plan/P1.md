# P1 · โครงการ + ภาพรวม — ตารางตรวจรับ

> เขียน**ก่อน**โค้ด ตาม `kp-acceptance-test-matrix`
> `tsc` เขียวและ `next build` เขียว **ไม่นับ**เป็นหลักฐานของแถวใดในนี้

**สถานะ:** `☐` ยังไม่ตรวจ · `✅` ผ่านโดยมีคำสั่งรันได้รองรับ · `❌` ตก · `⚠️` blocked · `👤` ต้องคนตรวจ

---

## 0 · สำรวจพื้นผิว

| นับอะไร | จากไหน | จำนวน |
|---|---|---|
| Routes | `/` (ภาพรวม), `/sites`, `/sites/[id]`, `/sites/new` | **4** |
| API endpoints × method | `POST /api/sites` · `PATCH /api/sites/[id]` · `POST`+`DELETE /api/sites/[id]/supervisors` · `POST`+`DELETE /api/sites/[id]/milestones` | **6** |
| Roles | `anon`, `owner`, `site_supervisor` | **3** |
| ตัวเลือก (enum) | `site_status` 5 ค่า: `planning` `active` `paused` `done` `cancelled` | **5** |
| ตาราง | `sites`, `site_supervisors`, `site_milestones`, `site_finance` | **4** |
| ฟังก์ชัน | `supervises_site(uuid, date)` · `site_overview(date)` | **2** |
| RLS policies | `sites` 4 · `site_supervisors` 4 · `site_milestones` 4 · `site_finance` 4 | **16** |
| **คอลัมน์ที่ต้องมีคนเขียน** | `name` `client_name` `client_phone` `address` `contract_amount` `start_date` `end_date` `status` · `effective_from` `effective_to` · `seq` `planned_amount` `planned_date` | **13** |

**อัตราส่วนที่ต้องปิด:** routes 4/4 · endpoints 6/6 · roles 3/3 · enum 5/5 · ตาราง 4/4 · policy 16/16 · คอลัมน์ 13/13 (เดิมนับ 14 เพราะนับ `name` สองครั้ง — ของ `sites` และของ `site_milestones` เป็นชื่อคอลัมน์เดียวกัน)

---

## P1-DB — schema และ RLS

| ID | Trigger | Role | UI | UX | API | DB | สถานะ |
|----|---------|------|----|----|-----|----|--------|
| P1-DB-01 | สร้างโครงการ | owner | n/a | n/a | n/a | `sites` **+1** · `audit_log` **+1** `action='INSERT'` | ✅ |
| P1-DB-02 | หัวหน้าโครงการสร้างโครงการเอง | site_supervisor | n/a | n/a | n/a | เขียนไม่ผ่าน — `sites` count **เท่าเดิม** *และ* owner สร้างได้ในคำสั่งถัดไป (พิสูจน์ว่าไม่ใช่ตารางพัง) | ✅ |
| P1-DB-03 | หัวหน้าโครงการอ่านโครงการที่ตัวเองดูแล | site_supervisor | n/a | n/a | n/a | เห็น **1 แถว** *และ* โครงการที่ไม่ได้ดูแลเห็น **0 แถว** ในการตรวจเดียวกัน | ✅ |
| P1-DB-04 | `anon` อ่าน `sites` | anon | n/a | n/a | n/a | **0 แถว** *และ* owner อ่านได้ **> 0** | ✅ |
| P1-DB-05 | หัวหน้าโครงการแก้ค่างานของโครงการตัวเอง | site_supervisor | n/a | n/a | n/a | `contract_amount` **ไม่เปลี่ยน** (แก้ได้เฉพาะ owner) | ✅ |
| P1-DB-06 | ทุกตารางใหม่เปิด RLS | n/a | n/a | n/a | n/a | `pg_tables` ที่ `rowsecurity=false` ใน public = **0** | ✅ |
| P1-DB-07 | คนเดียวอยู่สองโครงการในวันเดียวกัน | service | n/a | n/a | n/a | error ตรงกับ `/exclusion constraint\|conflicting key/` · แถวที่สอง **ไม่ถูกเขียน** | ✅ |
| P1-DB-08 | ช่วงเวลาที่ต่อกันพอดี (จบ 31 ม.ค. เริ่ม 1 ก.พ.) | service | n/a | n/a | n/a | **เขียนได้ทั้งสองแถว** — ถ้าตกแปลว่าลืม `+1` ตอนแปลง `effective_to` (ปลายทางแบบรวม) เป็น `daterange` (ปลายทางแบบไม่รวม) | ✅ |
| P1-DB-09 | ย้ายหัวหน้าโครงการล่วงหน้า (ตั้ง `effective_from` เป็นอนาคต) | owner | n/a | n/a | n/a | **วันนี้ยังเห็นโครงการเดิม** — "ปัจจุบัน" ต้องไม่ใช่ `effective_to is null` ไม่งั้นคนจะหายจากโครงการเดิมทันทีที่บันทึกการย้าย ทั้งที่ยังไม่ถึงวัน | ✅ |
| P1-DB-10 | `supervises_site()` ไม่มีทางลัด service-role | n/a | n/a | n/a | ซอร์ส (ตัดคอมเมนต์) **ไม่มี** `auth.uid() is null` | n/a | ✅ |
| P1-DB-11 | advisors หลัง migration | n/a | n/a | n/a | ERROR = **0** ทั้ง security และ performance | n/a | ✅ |
| P1-DB-12 | ทุกคอลัมน์ที่ไม่ใช่ generated มีโค้ดเขียน | n/a | 14 คอลัมน์ปรากฏใน payload ของ `.insert(`/`.update(` อย่างน้อยที่ละครั้ง | n/a | n/a | n/a | ✅ |

| P1-DB-13 | เรียก `site_overview(p_on)` | owner **และ** site_supervisor | n/a | n/a | n/a | เจ้าของได้ `active_count`≥ 2 · หัวหน้าโครงการได้ **1** และ `active_contract` = **1000000** (ไม่ใช่ 3000000) — ฟังก์ชันต้องเป็น `security invoker` ไม่ใช่ `definer` | ✅ |
| P1-DB-14 | หัวหน้าโครงการอ่าน `site_finance` ผ่าน PostgREST ตรง ๆ | site_supervisor | n/a | n/a | n/a | **0 แถว** *และ* เจ้าของอ่านได้ **≥ 1 แถว** ในการตรวจเดียวกัน | ✅ |

| P1-DB-15 | ตรวจ `information_schema.columns` | n/a | n/a | n/a | n/a | `sites.contract_amount` = **0 คอลัมน์** *และ* `site_finance.contract_amount` = **1 คอลัมน์** — ปิดทางอ้อมที่อ่านผ่านแถว `sites` ซึ่งหัวหน้าโครงการเห็นได้ | ✅ |
| P1-DB-16 | เรียก `site_overview()` | site_supervisor | n/a | n/a | n/a | `active_contract` = **`null`** ไม่ใช่ `0` *และ* เจ้าของได้ตัวเลขจริง — "ศูนย์" กับ "ไม่มีสิทธิ์เห็น" เป็นคนละเรื่อง หน้าจอซ่อนการ์ดที่ได้ null ได้ แต่จะวาด ฿0 อย่างมั่นใจ | ✅ |
| P1-DB-17 | สร้างโครงการ | owner | n/a | n/a | n/a | ทุกแถวใน `sites` มีคู่ใน `site_finance` — จำนวนโครงการที่ไม่มีแถวการเงิน = **0** (trigger `sites_ensure_finance`) | ✅ |

---

## P1-CALC — ความคืบหน้า 3 แถบ

> 🔴 **ห้ามให้แถวใดผ่านบน `0 === 0`** — ทุกแถวต้องสร้างข้อมูลเข้าไปก่อน
> โครงการที่ไม่มีรายการเลยจะได้ 0 ทุกช่อง ซึ่งตรงกันสามชั้นโดยไม่ได้พิสูจน์เลขสักตัว
> fixture ทุกตัวคืนค่าใน `finally`

| ID | Trigger | Role | UI | UX | API | DB | สถานะ |
|----|---------|------|----|----|-----|----|--------|
| P1-CALC-01 | โครงการที่เริ่ม 1 มี.ค. จบ 31 ส.ค. · วันนี้ 30 ส.ค. | owner | แถบเวลาแสดง **99%** (182/183 วัน) | n/a | n/a | n/a | ✅ |
| P1-CALC-02 | วันนี้อยู่ **ก่อน** วันเริ่ม | owner | แถบเวลา **0%** ไม่ใช่ค่าติดลบ | n/a | n/a | n/a | ✅ |
| P1-CALC-03 | วันนี้อยู่ **หลัง** วันจบ | owner | แถบเวลา **100%** ไม่เกิน 100 | n/a | n/a | n/a | ✅ |
| P1-CALC-04 | ไม่ได้ตั้งวันเริ่มหรือวันจบ | owner | แถบเวลาแสดง **"ยังไม่ได้ตั้งช่วงเวลา"** ไม่ใช่ 0% ที่อ่านเหมือนยังไม่เริ่ม | n/a | n/a | n/a | ✅ |
| P1-CALC-05 | `contract_amount = 0` | owner | ไม่หารด้วยศูนย์ — แถบเก็บเงิน/ต้นทุนแสดง **"ยังไม่ได้ตั้งค่างาน"** | n/a | n/a | n/a | ✅ |
| P1-CALC-06 | เขตเวลา — คำนวณ "วันนี้" | n/a | n/a | n/a | ใช้ `Asia/Bangkok` ทุกจุด · ตอน 23:30 น. ไทย ต้องยังนับเป็นวันเดียวกัน ไม่ใช่ข้ามไปพรุ่งนี้ตาม UTC | n/a | ⚠️ ตัดสินได้เฉพาะ 17:00–23:59 UTC · P1-CALC-06b ครอบกฎเดียวกันแบบแดงได้ทุกเวลา |
| P1-CALC-05b | งานวันเดียว (เริ่มและจบวันเดียวกัน) | owner | **1/1 วัน = 100%** ไม่ใช่ `NaN` จากการหารด้วยศูนย์ | n/a | n/a | n/a | ✅ |
| P1-CALC-06b | ค้นทั้ง `src/` (ตัดคอมเมนต์แล้ว) | n/a | n/a | n/a | **ไม่มีที่ไหน**หา "วันนี้" ด้วย `toISOString()` ซึ่งคืนวัน UTC — ผิดตั้งแต่ 7 โมงเช้าเวลาไทยของทุกวัน | n/a | ✅ |
| P1-CALC-07 | ป้ายเตือนต้นทุนแซงรายรับ | owner | โครงการที่ `ต้นทุน% > เก็บเงิน%` แสดงป้าย **"ต้นทุนโตเร็วกว่าเงินที่เก็บได้"** *และ* โครงการที่ยังไม่แซง **ไม่แสดง** ในหน้าเดียวกัน | n/a | n/a | n/a | ✅ (ปลดล็อกที่ P2-d2 · `verify-money.mjs`) |
| P1-CALC-08 | กำไรคงเหลือ | owner | = `ค่างานรวม − ต้นทุนที่เกิดขึ้นแล้ว` ตรงกับที่คำนวณจาก SQL ตรง ๆ | n/a | n/a | n/a | ✅ (ปลดล็อกที่ P2-d2 · `verify-money.mjs`) |

> **หมายเหตุ (ปิดแล้วที่ P2-d2):** ตอน P1 ยังไม่มีตาราง `transactions`
> แถบ "เก็บเงินแล้ว" กับ "ต้นทุน" จึงเป็น **0 จริง ๆ** และ P1-CALC-07/08 ถูก
> **`⚠️ blocked`** ไว้ ไม่ติ๊กด้วยเหตุผลว่า "สูตรถูกแล้ว" — สูตรที่ไม่เคยรับ
> input จริงยังไม่ได้ถูกทดสอบ · **ตอนนี้ปิดแล้ว** ด้วยข้อมูลจริงที่
> `scripts/verify-money.mjs` สร้างเข้าไป (ค่างาน ฿1,000,000 · รับแล้ว ฿400,000 ·
> ต้นทุน ฿300,000) แล้วคืนค่าใน `finally` · red-test แล้วทั้งสองแถว
> — ต้นทุนค่าแรงจาก `attendance` ยังรอ P4 และจะบวกเพิ่มเข้ามาในสูตรเดิม

---

## P1-UI — หน้าจอ

| ID | Trigger | Role | UI | UX | API | DB | สถานะ |
|----|---------|------|----|----|-----|----|--------|
| P1-UI-01 | เปิด `/sites` | owner | เห็นโครงการครบทุกโครงการที่มีในฐานข้อมูล | n/a | n/a | n/a | ✅ |
| P1-UI-02 | เปิด `/sites` | site_supervisor | เห็นเฉพาะโครงการที่ดูแล *และ* ชื่อโครงการที่ไม่ได้ดูแล**ไม่ปรากฏ**ในหน้าเดียวกัน | n/a | n/a | n/a | ✅ |
| P1-UI-03 | ยังไม่มีโครงการเลย | owner | สถานะว่าง: ข้อความ + ปุ่ม **"เพิ่มโครงการ"** | n/a | n/a | n/a | ✅ |
| P1-UI-03b | ค้นหาแล้วไม่เจอ | owner | ข้อความ **ไม่พบโครงการที่ตรงกับเงื่อนไขนี้** *และ* ข้อความ "ยังไม่มีโครงการในระบบ" **ไม่ปรากฏ** — สองสถานะนี้ต่างกัน ใช้ข้อความเดียวกันจะอ่านเหมือนข้อมูลหาย | n/a | n/a | n/a | ✅ |
| P1-UI-04 | เปิด `/sites/[id]` ของโครงการที่ไม่ได้ดูแล | site_supervisor | **404** ไม่ใช่หน้าเปล่า — หน้าเปล่าอ่านเหมือนระบบพัง | n/a | n/a | n/a | ✅ |
| P1-UI-05 | แต่ละค่าของ `site_status` (5 ค่า) | owner | ป้ายสถานะแสดงข้อความไทยครบทั้ง 5 ค่า ไม่มีค่าไหนแสดงเป็นค่าดิบภาษาอังกฤษ | n/a | n/a | n/a | ✅ |
| P1-UI-06 | สร้างโครงการใหม่ | owner | แถวใหม่โผล่ในรายการ | toast `เพิ่มโครงการแล้ว` · ปุ่ม disabled ระหว่างส่ง | `POST /api/sites` **201** | `sites` **+1** | ☐ (API/DB ผ่านที่ P1-API-09 · เหลือ toast+disabled รอ Playwright P8) |
| P1-UI-07 | สร้างโครงการโดยไม่ใส่ชื่อ | owner | ข้อความใต้ช่อง | ไม่มี toast สำเร็จ | **400** `body.error='NAME_REQUIRED'` | `sites` count **เท่าเดิม** | ☐ (API/DB ผ่านที่ P1-API-08 · เหลือข้อความใต้ช่อง รอ P8) |
| P1-UI-08 | double-click ปุ่มสร้าง | owner | มี **1 แถวใหม่เท่านั้น** | ปุ่ม disabled หลังคลิกแรก | คำขอที่สองไม่ถูกส่ง | `sites` **+1 เท่านั้น** | ☐ (กันกดซ้ำอยู่ฝั่ง client เท่านั้น — ต้องมีเบราว์เซอร์จริง รอ P8) |
| P1-UI-09 | หน้าภาพรวม `/` | owner | การ์ดสรุป 4 ใบ + รายการโครงการที่กำลังก่อสร้าง | n/a | n/a | n/a | ✅ |
| P1-UI-10 | หน้าภาพรวม `/` | site_supervisor | ไม่มีการ์ด "กำไรขั้นต้น" *และ* มีการ์ด "คนเข้าโครงการวันนี้" ในหน้าเดียวกัน | n/a | n/a | n/a | ⚠️ blocked (รอ P4) — ครึ่ง "ไม่มีการ์ดกำไร" ปิดแล้วที่ `P2-CALC-08` · เหลือการ์ด "คนเข้าโครงการวันนี้" ที่ยังไม่มีข้อมูล |
| P1-UI-10a | หน้าภาพรวม `/` | site_supervisor | เห็นชื่อโครงการที่ตัวเองดูแล *และ* **ไม่เห็น**ชื่อโครงการอื่นในหน้าเดียวกัน · คำบรรยายเป็น `ภาพรวมเฉพาะโครงการที่คุณดูแล` · ยอดค่างานรวมทั้งบริษัท (`3,000,000`) **ไม่ปรากฏ** | n/a | n/a | n/a | ✅ |
| P1-UI-13 | โครงการที่ `end_date` ผ่านไปแล้ว | owner | การ์ดนั้นขึ้นป้าย **เลยกำหนด** *และ* การ์ดของโครงการที่ยังอยู่ในกำหนด**ไม่ขึ้น**ป้ายนี้ ในหน้าเดียวกัน | n/a | n/a | n/a | ✅ |
| P1-UI-14 | เปิด `/sites/[id]` ของโครงการที่ตัวเองดูแล | site_supervisor | ตัวเลขค่างาน (`3,500,000`) และหัวข้อ `ค่างานตามสัญญา` **ไม่ปรากฏ** *และ* เจ้าของเห็นทั้งคู่ในโครงการเดียวกัน · 🔴 นี่เป็นแค่ชั้นหน้าจอ ชั้นที่บังคับจริงคือ P1-DB-14 | n/a | n/a | n/a | ✅ |
| P1-UI-11 | ที่ความกว้าง 390px | owner | ไม่มีการเลื่อนออกด้านข้าง · การ์ดเรียงเป็นคอลัมน์เดียว | n/a | n/a | n/a | 👤 (`scrollWidth <= innerWidth` ต้องมี viewport จริง รอ Playwright P8 พร้อม P0-UI-11) |

---

| P1-UI-12 | เปิด `/sites/[id]` หลังแก้ข้อมูล | owner | หน้าแสดง **ชื่อใหม่** · ป้ายสถานะไทย `หยุดชั่วคราว` · ค่างาน `3,500,000` · ชื่องวด · ชื่อหัวหน้าโครงการ · เบอร์ลูกค้า — ครบ 6 อย่าง | n/a | n/a | n/a | ✅ |

---

## P1-API — ขอบเขต endpoint

| ID | Trigger | Role | UI | UX | API | DB | สถานะ |
|----|---------|------|----|----|-----|----|--------|
| P1-API-01 | ทุก endpoint ตอนไม่ล็อกอิน | anon | n/a | n/a | **401 JSON** ทุกตัว (ไม่ใช่ 307) | ไม่มีแถวเปลี่ยน | ✅ |
| P1-API-01a | `POST /api/sites` ตอนไม่ล็อกอิน | anon | n/a | n/a | **401** + `content-type: application/json` — ไม่ใช่ 307 ไปหน้า login | ไม่มีแถวเปลี่ยน | ✅ |
| P1-API-07 | `startDate` เป็นปี **พ.ศ.** (`2569-08-30`) | owner | n/a | n/a | **400** `body.error='DATE_BUDDHIST_ERA'` — `2569-08-30` ถูกไวยากรณ์ Postgres จึงถูกรับไว้เงียบ ๆ แล้วเพี้ยน 543 ปี | `sites` count **เท่าเดิม** | ✅ |
| P1-API-08 | ชื่อเป็นช่องว่างล้วน | owner | n/a | n/a | **400** `body.error='NAME_REQUIRED'` | `sites` count **เท่าเดิม** | ✅ |
| P1-API-09 | สร้างโครงการสำเร็จ | owner | ชื่อโครงการใหม่ปรากฏใน HTML ของ `/sites` | n/a | **201** `body.site.id` เป็น uuid | `sites` **+1** · `audit_log` **+1** `action='INSERT'` | ✅ |
| P1-API-09b | ส่งค่างานเป็นข้อความมีจุลภาค `"1,500,000"` | owner | n/a | n/a | n/a | `contract_amount` = **1500000** (ไม่ใช่ 0 หรือ 1) · `start_date` = **`2026-03-01`** (ค.ศ.) | ✅ |
| P1-API-02 | `POST /api/sites` | site_supervisor | n/a | n/a | **403 FORBIDDEN** | `sites` count **เท่าเดิม** | ✅ |
| P1-API-03 | `PATCH /api/sites/[id]` โครงการที่ไม่มีอยู่ | owner | n/a | n/a | **404 NOT_FOUND** | n/a | ✅ |
| P1-API-04 | `contract_amount` เป็นค่าติดลบ | owner | n/a | n/a | **400** `body.error='AMOUNT_INVALID'` | ไม่มีแถวเปลี่ยน | ✅ |
| P1-API-05 | `end_date` มาก่อน `start_date` | owner | n/a | n/a | **400** `body.error='DATE_RANGE_INVALID'` | ไม่มีแถวเปลี่ยน | ✅ |
| P1-API-06 | มอบหมายหัวหน้าโครงการที่ทับช่วงเวลาเดิม | owner | n/a | n/a | **409** `body.error='OVERLAP'` | ไม่มีแถวใหม่ | ✅ |

---

| P1-API-10 | `PATCH /api/sites/[id]` โครงการที่ตัวเองดูแล | site_supervisor | n/a | n/a | **403 FORBIDDEN** | `name` และ `contract_amount` **ไม่เปลี่ยน** | ✅ |
| P1-API-11 | `PATCH /api/sites/[id]` ครบทุกฟิลด์ | owner | n/a | n/a | **200** | ทั้ง 8 คอลัมน์ถูกเขียนจริง (`status='paused'` · `3500000` · `end_date='2026-09-30'` · เบอร์ · ที่อยู่) | ✅ |
| P1-API-12 | มอบหมายช่วงที่ต่อกันพอดี (จบ 31 พ.ค. เริ่ม 1 มิ.ย.) | owner | n/a | n/a | **201** — กฎที่ถูกที่ชั้นฐานข้อมูลแต่ผิดที่ชั้น API ก็ยังผิด | แถวใหม่ **+1** | ✅ |
| P1-API-13 | เพิ่มงวดโดยเว้นเลขงวด แล้วเพิ่มงวดเลขซ้ำ | owner | n/a | n/a | เว้นเลข → `seq` **1** แล้ว **2** · เลขซ้ำ → **409** `SEQ_TAKEN` | `site_milestones` = **2 แถว** · `sum(planned_amount)` = **1900000** | ✅ |

## สรุปความครอบคลุม

```
รวม 58 แถว                                     (P1 ปิดครบทั้งเฟส)
  ✅ ผ่านโดยมีคำสั่งรันได้รองรับ : 52
  ☐  ยังไม่ตรวจ                  : 3
  ⚠️ ตัดสินไม่ได้ / รอข้อมูลจริง   : 2
  👤 ต้องมีเบราว์เซอร์จริง รอ P8  : 1
  ❌ ตก                          : 0
```

**แถวที่ยังไม่ปิด และเหตุผล** — ไม่มีแถวไหนถูกลบเพื่อให้ตารางดูสวย:
- `P1-CALC-06` ตัดสินได้เฉพาะ 17:00–23:59 UTC · `P1-CALC-06b` ครอบกฎเดียวกันแบบแดงได้ทุกเวลา
- ~~`P1-CALC-07` `P1-CALC-08` รอ `transactions`~~ **ปิดแล้วที่ P2-d2** (`verify-money.mjs`)
- `P1-UI-10` ยังรอ `attendance` (P4) — ครึ่งที่เป็นเรื่องกำไรปิดแล้วที่ `P2-CALC-08`
- `P1-UI-06/07/08` `P1-UI-11` ต้องมีเบราว์เซอร์จริง (toast · ปุ่ม disabled · กดซ้ำ · viewport) → P8
- ~~`P1-DB-14` ช่องว่างที่รู้ตัว~~ **ปิดแล้ว** — เจ้าของตัดสินว่าค่างานเป็นความลับ
  จึงย้าย `contract_amount` ไปตาราง `site_finance` ที่เจ้าของเท่านั้นอ่านได้
  (migration `20260830170000`) · ปิดครบสามทาง: ตาราง `sites` ไม่มีคอลัมน์นี้แล้ว (`P1-DB-15`)
  · RPC คืน `null` ไม่ใช่ 0 (`P1-DB-16`) · หน้าจอไม่แสดง (`P1-UI-14`)

**หลักฐาน:** `node scripts/verify-sites.mjs` (11) · `node scripts/verify-sites-api.mjs` (32)
กระทบยอดอัตโนมัติด้วย `node scripts/verify-matrix.mjs`

**red-test ที่ทำไปแล้ว** (ตัวตรวจที่แดงไม่เป็น ยังไม่นับเป็นตัวตรวจ):
- สลับหน้า `/sites` ไปใช้ secret key → **P1-UI-02 แดง**
- เปลี่ยนป้าย `paused` เป็นค่าดิบภาษาอังกฤษ → **P1-UI-05 แดง**
- ลบ `planned_date` ออกจาก payload ของ `POST …/milestones` → **P1-DB-12 แดง**
- เปิด policy ของ `site_finance` ให้ทุกคนที่ล็อกอินอ่านได้ → **P1-DB-14 แดง**
  (หัวหน้าโครงการอ่านได้ 2 แถวแทนที่จะเป็น 0 — คืน policy แล้วเขียวกลับ)
- เปลี่ยน `site_overview()` เป็น `security definer` → **P1-DB-13 และ P1-UI-10a แดง**
  (หัวหน้าโครงการเห็นค่างานรวม **฿3,000,000** ของทั้งบริษัทแทนที่จะเป็น ฿1,000,000 ของตัวเอง
  โดยหน้าจอดูปกติทุกอย่าง — คืนค่าเดิมแล้วเขียวกลับ)
  (ครั้งแรก **ไม่แดง** — เจอว่า `\s*` ก่อน negative lookahead ถอยกลับไปแมตช์ศูนย์ตัวได้
  ทำให้ lookahead ไปตรวจที่ตัวเว้นวรรคแล้วผ่านตลอด · แก้เป็นการจับค่าออกมาเทียบตรง ๆ)

**กฎการติ๊ก** — แถวที่ ID มี `-DB-` `-CALC-` `-API-` ต้องมี ID อยู่ใน label ของ check
ที่รันได้จริงใน `scripts/**` · ติ๊กจากความจำไม่นับ
**และห้ามติ๊กแถวคำนวณที่ input ยังเป็นศูนย์** — ต้องสร้างข้อมูลเข้าไปก่อน แล้วคืนใน `finally`
